/**
 * Shared types for frontend-api package (Connect-RPC)
 */

// Все типы для API requests/responses теперь приходят из @axion/contracts
// Здесь только вспомогательные типы

  /**
 * Re-export Connect-RPC types for convenience
 */
export type { Client, Transport, ConnectError } from "@connectrpc/connect";
