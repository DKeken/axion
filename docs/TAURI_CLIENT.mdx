---
title: "Tauri Client"
description: "Нативное desktop приложение на Tauri v2 с локальным режимом: работа без установки сервера, нативная интеграция с Runner Agent."
order: 6
---

# Tauri Client

<Callout type="idea" title="Уникальная фича">
  Tauri Client предоставляет уникальную возможность локальной разработки с
  прямой интеграцией с Runner Agent через нативные Rust интерфейсы.
</Callout>

<Callout type="info" title="Связанные документы">
  - [Архитектура](./ARCHITECTURE) — раздел "Клиенты" описывает веб и Tauri
  клиенты - [Runner Agent](./RUNNER_AGENT) — Архитектура Runner Agent для
  локальной интеграции - [Технические детали](./TECHNICAL_DETAILS) — Технические
  детали реализации
</Callout>

## Обзор

Axion Tauri Client — это **нативное desktop приложение** для Axion Meta-Framework, построенное на **Tauri v2** (Rust + Web Frontend). В отличие от веб-версии, Tauri клиент предоставляет **уникальную возможность локальной разработки** с прямой интеграцией с **Axion Runner Agent** через нативные Rust интерфейсы.

**Ключевые особенности:**

| Особенность                     | Описание                                          | Преимущества                        |
| ------------------------------- | ------------------------------------------------- | ----------------------------------- |
| **Нативное Desktop приложение** | Tauri v2 для всех платформ                        | Windows, macOS, Linux               |
| **Локальный режим**             | Работа без установки сервера                      | Runner Agent локально на компьютере |
| **Нативная интеграция**         | Прямое общение через IPC/Unix sockets             | Оба компонента на Rust              |
| **Высокая производительность**  | Нативные компоненты на Rust, быстрый UI на React  | Минимальная задержка                |
| **Безопасность**                | Минимальная поверхность атаки, изоляция процессов | Защита от уязвимостей               |
| **Локальное кэширование**       | Локальное хранение проектов и графов              | Быстрый доступ                      |
| **Единый UI**                   | Тот же React Flow редактор, что и в веб-версии    | Консистентность                     |
| **Интеграция с Control Plane**  | Использование SaaS для генерации кода             | Гибридная работа                    |

**Уникальная фича — Локальный режим:**

Главное преимущество Tauri клиента — **работа без необходимости устанавливать и настраивать сервер**. Runner Agent работает **локально на вашем компьютере**:

| Сценарий                     | Описание                                               | Преимущества              |
| ---------------------------- | ------------------------------------------------------ | ------------------------- |
| **Локальная разработка**     | Разработка и тестирование проектов на своем компьютере | Не нужен удаленный сервер |
| **Быстрое прототипирование** | Мгновенный локальный деплой через Docker Swarm         | Быстрый цикл разработки   |
| **Эксперименты**             | Безопасное тестирование без влияния на production      | Изоляция от production    |
| **Разработка без сервера**   | Не нужно настраивать SSH, подключать удаленные серверы | Простота настройки        |
| **Гибридная работа**         | Локальная разработка + синхронизация с Control Plane   | Гибкость                  |

**Нативная интеграция с Runner Agent:**

Оба компонента написаны на **Rust**, что позволяет им общаться напрямую:

| Механизм                | Платформа   | Преимущества                          |
| ----------------------- | ----------- | ------------------------------------- |
| **Unix Domain Sockets** | Linux/macOS | Быстрая локальная коммуникация        |
| **Named Pipes**         | Windows     | Аналог Unix sockets для Windows       |
| **gRPC локально**       | Все         | Типобезопасное общение через Protobuf |
| **Shared Memory**       | Все         | Для передачи больших объемов данных   |

Это обеспечивает **мгновенную реакцию** и **минимальную задержку** при локальной работе.

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────────────┐
│              Axion Tauri Client (Desktop Application)                 │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Frontend: React + TypeScript (WebView)                       │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  React Flow Visual Editor                                │  │  │
│  │  │  - Drag & Drop сервисов                                  │  │  │
│  │  │  - Визуальные связи (HTTP RPC, HTTP, DB)                 │  │  │
│  │  │  - Контекстное меню для создания                         │  │  │
│  │  │  - Real-time синхронизация (локальная)                   │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Project Manager                                         │  │  │
│  │  │  - Локальное хранение проектов (SQLite)                 │  │  │
│  │  │  - Управление графами                                    │  │  │
│  │  │  - История версий                                        │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Code Generation UI                                      │  │  │
│  │  │  - LLM чат для генерации бизнес-логики                  │  │  │
│  │  │  - Превью сгенерированного кода                         │  │  │
│  │  │  - Валидация кода                                        │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Local Runner Manager                                    │  │  │
│  │  │  - Управление локальным Runner Agent                     │  │  │
│  │  │  - Мониторинг локальных сервисов                        │  │  │
│  │  │  - Локальный деплой через Runner Agent                  │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Tauri Backend: Rust                                           │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Project Service (Rust)                                 │  │  │
│  │  │  - CRUD проектов в SQLite                               │  │  │
│  │  │  - Управление графами (JSON)                            │  │  │
│  │  │  - Версионирование                                      │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Codegen Service (Rust)                                 │  │  │
│  │  │  - Генерация кода из графов                             │  │  │
│  │  │  - LLM Integration (OpenRouter)                         │  │  │
│  │  │  - Blueprint System                                      │  │  │
│  │  │  - Template Engine                                       │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Runner Agent Client (Rust)                            │  │  │
│  │  │  - Нативное общение с локальным Runner Agent           │  │  │
│  │  │  - Unix Domain Sockets / Named Pipes                    │  │  │
│  │  │  - gRPC локально (опционально)                          │  │  │
│  │  │  - Управление локальным Docker Swarm                    │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  File System Service (Rust)                            │  │  │
│  │  │  - Чтение/запись файлов проектов                        │  │  │
│  │  │  - Управление сгенерированным кодом                     │  │  │
│  │  │  - Интеграция с IDE (VS Code, etc.)                     │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Sync Service (Rust)                                    │  │  │
│  │  │  - Опциональная синхронизация с Control Plane           │  │  │
│  │  │  - Конфликт-резолюшн                                    │  │  │
│  │  │  - Инкрементальная синхронизация                        │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  LLM Service (Rust)                                     │  │  │
│  │  │  - Интеграция с OpenRouter API                          │  │  │
│  │  │  - Кэширование промптов                                 │  │  │
│  │  │  - Rate limiting                                        │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ IPC / Unix Sockets / Named Pipes
┌─────────────────────────────────────────────────────────────────────┐
│              Local Axion Runner Agent (Rust Binary)                  │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  IPC Server                                                    │  │
│  │  - Unix Domain Socket (Linux/macOS)                           │  │
│  │  - Named Pipe (Windows)                                        │  │
│  │  - gRPC локально (опционально)                                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Docker Swarm Manager                                          │  │
│  │  - Управление локальным Docker Swarm                          │  │
│  │  - Rolling updates                                             │  │
│  │  - Health checks                                               │  │
│  │  - Scaling сервисов                                            │  │
│  └───────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Local Telemetry Collector                                    │  │
│  │  - Сбор метрик локальных сервисов                             │  │
│  │  - Отправка в Tauri Client через IPC                          │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ Опционально
┌─────────────────────────────────────────────────────────────────────┐
│              Axion Control Plane (SaaS) - Опционально                │
│                                                                       │
│  - Синхронизация проектов                                            │
│  - Облачное хранение                                                 │
│  - Совместная работа                                                 │
│  - Production деплой                                                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Режимы работы

### 1. Локальный Режим (Local Development)

**Работа без установки сервера — Runner Agent на вашем компьютере:**

- ✅ **Локальный Runner Agent** — работает на вашем компьютере, управляет локальным Docker Swarm
- ✅ **Локальное тестирование** — деплой и тестирование проектов локально через Docker
- ✅ **Кэширование проектов** — локальное хранение проектов в SQLite для быстрого доступа
- ✅ **Интеграция с Control Plane** — использование SaaS для генерации кода, хранения проектов, управления
- ✅ **LLM через Control Plane** — генерация бизнес-логики через Control Plane (или напрямую к OpenRouter)
- ✅ **Не нужен сервер** — главная фича: не нужно настраивать SSH, подключать удаленные серверы

**Использование:**

- Локальная разработка проектов на своем компьютере
- Быстрое прототипирование с локальным деплоем
- Эксперименты без влияния на production серверы
- Разработка без необходимости настраивать инфраструктуру

### 2. Гибридный Режим (Hybrid)

**Комбинация локальной разработки и production деплоя:**

- ✅ **Локальная разработка** — разработка и тестирование на своем компьютере через локальный Runner Agent
- ✅ **Production деплой** — деплой на удаленные серверы через Control Plane
- ✅ **Синхронизация проектов** — автоматическая синхронизация между локальной и облачной версиями
- ✅ **Конфликт-резолюшн** — разрешение конфликтов при расхождении версий
- ✅ **Выборочная синхронизация** — выбор проектов для синхронизации с Control Plane

**Использование:**

- Разработка локально, production деплой в облако
- Совместная работа над проектами
- Backup проектов в облаке
- Гибридная работа: локальная разработка + облачное управление

### 3. Облачный Режим (Cloud)

**Полная интеграция с Control Plane (как веб-версия):**

- ✅ Все проекты хранятся в Control Plane
- ✅ Синхронизация в реальном времени
- ✅ Совместная работа
- ✅ Production деплой на удаленные серверы клиентов через Runner Agent на серверах

**Использование:**

- Production проекты
- Командная работа
- Управление удаленными серверами и кластерами
- Полная облачная работа (как веб-версия)

---

## 🔗 Нативная Интеграция с Runner Agent

### Концепция

Оба компонента (Tauri Client и Runner Agent) написаны на **Rust**, что позволяет им общаться напрямую через нативные интерфейсы операционной системы без HTTP/gRPC overhead.

### Механизмы Коммуникации

#### 1. Unix Domain Sockets (Linux/macOS)

**Самый быстрый способ для Linux/macOS:**

```rust
// Tauri Client (Rust)
use tokio::net::UnixStream;

async fn connect_to_runner_agent() -> Result<UnixStream> {
    let socket_path = "/tmp/axion-runner-agent.sock";
    UnixStream::connect(socket_path).await
}

// Runner Agent (Rust)
use tokio::net::UnixListener;

async fn start_ipc_server() -> Result<()> {
    let socket_path = "/tmp/axion-runner-agent.sock";
    let listener = UnixListener::bind(socket_path)?;

    loop {
        let (stream, _) = listener.accept().await?;
        // Обработка команд от Tauri Client
    }
}
```

**Преимущества:**

- ⚡ **Мгновенная реакция** — нет сетевых задержек
- 🔒 **Безопасность** — доступ только с локальной машины
- 📦 **Минимальный overhead** — нет HTTP/gRPC заголовков
- 🚀 **Высокая производительность** — прямая передача данных

#### 2. Named Pipes (Windows)

**Аналог Unix sockets для Windows:**

```rust
// Tauri Client (Rust)
use tokio::net::windows::named_pipe::ClientOptions;

async fn connect_to_runner_agent() -> Result<NamedPipeClient> {
    let pipe_name = r"\\.\pipe\axion-runner-agent";
    ClientOptions::new().open(pipe_name).await
}

// Runner Agent (Rust)
use tokio::net::windows::named_pipe::{ServerOptions, NamedPipeServer};

async fn start_ipc_server() -> Result<()> {
    let pipe_name = r"\\.\pipe\axion-runner-agent";
    let server = ServerOptions::new()
        .first_pipe_instance(true)
        .create(pipe_name)?;

    // Обработка команд от Tauri Client
}
```

#### 3. gRPC Локально (Опционально)

**Для типобезопасности и совместимости с Control Plane:**

```rust
// Tauri Client (Rust)
use tonic::transport::Channel;

async fn connect_to_runner_agent_grpc() -> Result<RunnerAgentClient<Channel>> {
    let channel = Channel::from_static("http://localhost:50051")
        .connect()
        .await?;

    RunnerAgentClient::new(channel)
}
```

**Использование:**

- Когда нужна полная совместимость с Control Plane протоколом
- Для сложных типов данных (Protobuf)
- Для будущей интеграции с облачными агентами

### Протокол Коммуникации

**Бинарный протокол через IPC:**

```rust
// Команда от Tauri Client к Runner Agent
#[derive(Serialize, Deserialize)]
pub enum RunnerCommand {
    Deploy {
        project_id: String,
        docker_stack_yml: String,
        env_vars: HashMap<String, String>,
    },
    GetStatus {
        project_id: String,
    },
    GetMetrics {
        project_id: String,
    },
    ScaleService {
        project_id: String,
        service_name: String,
        replicas: u32,
    },
    StopProject {
        project_id: String,
    },
}

// Ответ от Runner Agent к Tauri Client
#[derive(Serialize, Deserialize)]
pub enum RunnerResponse {
    DeployStatus {
        status: DeployStatus,
        message: String,
    },
    ProjectStatus {
        services: Vec<ServiceStatus>,
    },
    Metrics {
        metrics: ProjectMetrics,
    },
    Error {
        error: String,
    },
}
```

**Сериализация:**

- **bincode** — быстрая бинарная сериализация для IPC
- **Protobuf** — для gRPC (опционально)
- **JSON** — для отладки и логирования

### Преимущества Нативной Интеграции

1. **⚡ Мгновенная реакция** — нет сетевых задержек, прямая передача данных
2. **🔒 Безопасность** — доступ только с локальной машины, нет сетевых уязвимостей
3. **📦 Минимальный overhead** — нет HTTP/gRPC заголовков, бинарный протокол
4. **🚀 Высокая производительность** — прямая передача данных через IPC
5. **🛠️ Простота отладки** — локальная работа, легче отлаживать
6. **💾 Эффективность памяти** — shared memory для больших данных (опционально)

---

## Локальное кэширование

### SQLite Database

**Проекты кэшируются локально в SQLite для быстрого доступа:**

```sql
-- Проекты (кэш из Control Plane)
CREATE TABLE projects (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  graph_data BLOB NOT NULL, -- JSON графа (кэш)
  graph_version INTEGER DEFAULT 1,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  synced_at INTEGER, -- Время последней синхронизации с Control Plane
  sync_enabled INTEGER DEFAULT 1 -- По умолчанию включена синхронизация
);

-- Сервисы проекта
CREATE TABLE project_services (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  node_id TEXT NOT NULL,
  service_name TEXT NOT NULL,
  blueprint_id TEXT NOT NULL,
  config BLOB NOT NULL, -- JSON конфигурации
  generated_code_path TEXT,
  code_version INTEGER DEFAULT 1,
  status TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  UNIQUE(project_id, node_id)
);

-- Версии графов
CREATE TABLE project_graph_versions (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  version INTEGER NOT NULL,
  graph_data BLOB NOT NULL,
  created_at INTEGER NOT NULL
);

-- Локальные деплои
CREATE TABLE local_deployments (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  status TEXT NOT NULL,
  docker_stack_yml TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
```

**Преимущества локального кэширования:**

- ✅ **Быстрый доступ** — мгновенный доступ к проектам без запросов к Control Plane
- ✅ **Офлайн просмотр** — просмотр кэшированных проектов без интернета
- ✅ **Производительность** — быстрая работа UI без задержек сети
- ✅ **Надежность** — ACID транзакции, проверенная временем
- ✅ **Синхронизация** — автоматическая синхронизация с Control Plane при подключении

### Файловая Структура

```
~/.axion/
├── database.sqlite          # SQLite база данных
├── projects/
│   ├── {project_id}/
│   │   ├── graph.json       # Граф проекта
│   │   ├── generated/       # Сгенерированный код
│   │   │   ├── apps/
│   │   │   ├── packages/
│   │   │   └── ...
│   │   └── versions/        # Версии графов
│   └── ...
├── cache/
│   ├── blueprints/          # Кэш blueprints
│   ├── templates/           # Кэш шаблонов
│   └── llm/                 # Кэш LLM ответов
└── logs/
    └── axion-client.log     # Логи приложения
```

---

## Синхронизация с Control Plane

### Опциональная Синхронизация

Tauri клиент может **опционально** синхронизироваться с Control Plane для:

- 📦 **Backup проектов** — резервное копирование в облаке
- 👥 **Совместной работы** — работа в команде над проектами
- 🚀 **Production деплоя** — деплой на серверы клиентов
- 📊 **Метрик и мониторинга** — централизованный мониторинг

### Процесс Синхронизации

#### 1. Инкрементальная Синхронизация

**Только изменения, а не весь проект:**

```rust
// Определение изменений
struct ProjectDiff {
    project_id: String,
    graph_changes: Vec<GraphChange>,
    service_changes: Vec<ServiceChange>,
    code_changes: Vec<CodeChange>,
}

// Синхронизация
async fn sync_project(project_id: String) -> Result<()> {
    // 1. Получить локальную версию
    let local_project = db.get_project(project_id)?;

    // 2. Получить облачную версию
    let cloud_project = api.get_project(project_id).await?;

    // 3. Вычислить diff
    let diff = compute_diff(&local_project, &cloud_project);

    // 4. Разрешить конфликты
    let resolved = resolve_conflicts(diff)?;

    // 5. Применить изменения
    apply_changes(resolved).await?;

    // 6. Обновить sync timestamp
    db.update_sync_time(project_id)?;
}
```

#### 2. Конфликт-Резолюшн

**При расхождении версий:**

```rust
enum ConflictResolution {
    UseLocal,      // Использовать локальную версию
    UseCloud,      // Использовать облачную версию
    Merge,         // Объединить изменения
    Manual,        // Ручное разрешение
}

// UI для разрешения конфликтов
async fn resolve_conflict(
    conflict: Conflict,
) -> Result<ConflictResolution> {
    // Показать UI с опциями:
    // - Использовать локальную версию
    // - Использовать облачную версию
    // - Объединить изменения
    // - Разрешить вручную
}
```

#### 3. Выборочная Синхронизация

**Пользователь выбирает, что синхронизировать:**

```rust
struct SyncSettings {
    project_id: String,
    sync_enabled: bool,
    sync_graph: bool,
    sync_code: bool,
    sync_deployments: bool,
    auto_sync: bool, // Автоматическая синхронизация при изменениях
}
```

### Режимы Синхронизации

1. **Автоматическая** — синхронизация при каждом изменении
2. **По запросу** — синхронизация только при нажатии кнопки
3. **По расписанию** — синхронизация по таймеру (например, каждый час)
4. **При подключении** — синхронизация при подключении к интернету

---

## 🚀 Генерация Кода через Control Plane

### Процесс Генерации

**Генерация кода выполняется через Control Plane SaaS:**

```rust
// Tauri Command для генерации кода
#[tauri::command]
async fn generate_project_code(
    project_id: String,
    app_handle: AppHandle,
) -> Result<GenerateResult> {
    // 1. Загрузить граф проекта (локально из SQLite или из Control Plane)
    let graph = db.get_project_graph(project_id)?;

    // 2. Отправить запрос на генерацию в Control Plane
    let generate_request = GenerateRequest {
        project_id: project_id.clone(),
        graph_data: graph,
    };

    // 3. Control Plane генерирует код
    let generate_result = control_plane_api
        .generate_code(generate_request)
        .await?;

    // 4. Сохранить сгенерированный код локально
    file_service.save_generated_code(
        project_id,
        generate_result.code_path,
    )?;

    // 5. Кэшировать результат локально
    db.cache_generated_code(project_id, generate_result)?;

    Ok(GenerateResult {
        services_generated: generate_result.services_generated,
        validation_errors: generate_result.validation_errors,
    })
}
```

### Альтернатива: Прямая LLM Интеграция (Опционально)

**Для работы без Control Plane (опционально, требует настройки API ключей):**

```rust
// LLM Service (Rust) - опционально, если не использовать Control Plane
pub struct LLMService {
    openrouter_client: Option<OpenRouterClient>,
    use_control_plane: bool, // По умолчанию true
}

impl LLMService {
    async fn generate_logic(
        &self,
        instructions: String,
        config: BlueprintConfig,
    ) -> Result<String> {
        if self.use_control_plane {
            // Использовать Control Plane для генерации
            control_plane_api.generate_logic(instructions, config).await
        } else {
            // Прямое подключение к OpenRouter (требует API ключ)
            let prompt = format!("{}", instructions);

            if let Some(openrouter) = &self.openrouter_client {
                openrouter.generate(prompt, config.model_id).await
            } else {
                Err("No LLM client configured".into())
            }
        }
    }
}
```

**Настройка:**

- **По умолчанию:** использование Control Plane для генерации кода
- **Опционально:** прямое подключение к OpenRouter (требует настройки API ключа)
- API ключ хранится в зашифрованном виде в локальном хранилище
- OpenRouter предоставляет единый API для доступа к множеству моделей (OpenAI, Anthropic, Google, Mistral и др.)

---

## 🐳 Локальный Деплой через Runner Agent

### Процесс Деплоя

**Прямое общение с локальным Runner Agent:**

```rust
// Tauri Command для деплоя
#[tauri::command]
async fn deploy_project_locally(
    project_id: String,
    app_handle: AppHandle,
) -> Result<DeployResult> {
    // 1. Загрузить проект
    let project = db.get_project(project_id)?;

    // 2. Генерировать Docker Swarm Stack
    let docker_stack = generate_docker_stack(&project)?;

    // 3. Подключиться к локальному Runner Agent
    let runner_client = runner_agent_client.connect().await?;

    // 4. Отправить команду деплоя через IPC
    let deploy_command = RunnerCommand::Deploy {
        project_id: project_id.clone(),
        docker_stack_yml: docker_stack,
        env_vars: project.env_vars,
    };

    let response = runner_client.send_command(deploy_command).await?;

    // 5. Мониторинг деплоя
    match response {
        RunnerResponse::DeployStatus { status, message } => {
            // Обновить UI через события
            app_handle.emit("deploy-status", status)?;

            Ok(DeployResult {
                status,
                message,
            })
        }
        RunnerResponse::Error { error } => {
            Err(error.into())
        }
    }
}
```

### Мониторинг Локальных Сервисов

**Real-time метрики через IPC:**

```rust
// Подписка на метрики от Runner Agent
async fn subscribe_to_metrics(
    project_id: String,
    app_handle: AppHandle,
) -> Result<()> {
    let runner_client = runner_agent_client.connect().await?;

    // Подписка на метрики через IPC stream
    let mut metrics_stream = runner_client.subscribe_metrics(project_id).await?;

    while let Some(metrics) = metrics_stream.next().await {
        // Отправить метрики в UI через Tauri события
        app_handle.emit("metrics-update", metrics)?;
    }

    Ok(())
}
```

---

## UI Компоненты

### React Flow Editor

**Тот же редактор, что и в веб-версии:**

- Drag & Drop сервисов
- Визуальные связи (HTTP RPC, HTTP, DB)
- Контекстное меню для создания
- Real-time синхронизация (локальная)

### Project Manager

**Управление локальными проектами:**

- Список проектов из SQLite
- Создание/удаление проектов
- Открытие проектов в редакторе
- Настройки синхронизации

### Local Runner Manager

**Управление локальным Runner Agent:**

- Статус Runner Agent (запущен/остановлен)
- Управление локальными сервисами
- Мониторинг метрик
- Логи локальных сервисов

### Code Generation UI

**Генерация и просмотр кода:**

- LLM чат для генерации бизнес-логики
- Превью сгенерированного кода
- Валидация кода
- Открытие в IDE (VS Code, etc.)

---

## 🔐 Безопасность

### Локальное Хранение

- ✅ **Шифрование** — API ключи и секреты хранятся в зашифрованном виде
- ✅ **Изоляция** — каждый проект изолирован
- ✅ **Минимальные привилегии** — Tauri использует минимальные системные привилегии

### IPC Безопасность

- ✅ **Unix Domain Sockets** — доступ только с локальной машины
- ✅ **Права доступа** — socket файлы с ограниченными правами (600)
- ✅ **Валидация команд** — проверка всех команд от Tauri Client

### Синхронизация

- ✅ **TLS** — все соединения с Control Plane через TLS
- ✅ **Аутентификация** — токены для аутентификации
- ✅ **Шифрование данных** — опциональное шифрование данных при синхронизации

---

## Производительность

### Оптимизации

1. **Локальное хранение** — SQLite для быстрого доступа
2. **Кэширование** — кэш blueprints, шаблонов, LLM ответов
3. **Ленивая загрузка** — загрузка проектов по требованию
4. **Инкрементальная синхронизация** — только изменения
5. **Нативная IPC** — мгновенная реакция без сетевых задержек

### Метрики

- **Время открытия проекта:** < 100ms
- **Время генерации кода:** зависит от LLM API
- **Время деплоя:** < 5 секунд (локально)
- **Потребление памяти:** ~200-300MB (Tauri + React)
- **Потребление CPU:** минимальное в idle, пики при генерации

---

## Разработка

### Технологический Стек

**Frontend:**

- React 19 + TypeScript
- React Flow для визуального редактора
- Tailwind CSS для стилей
- Zustand для state management

**Backend (Tauri):**

- Rust
- SQLite (rusqlite) для локального хранения
- tokio для асинхронности
- serde для сериализации
- tonic для gRPC (опционально)

**Интеграции:**

- Axion Runner Agent (локальный)
- OpenRouter API (LLM)
- Control Plane API (синхронизация)

### Структура Проекта

```
apps/tauri-client/
├── src/
│   ├── main.rs              # Tauri entry point
│   ├── commands/            # Tauri commands
│   │   ├── projects.rs
│   │   ├── codegen.rs
│   │   ├── runner.rs
│   │   └── sync.rs
│   ├── services/            # Rust services
│   │   ├── project_service.rs
│   │   ├── codegen_service.rs
│   │   ├── runner_agent_client.rs
│   │   ├── llm_service.rs
│   │   └── sync_service.rs
│   └── database/            # SQLite database
│       └── schema.rs
├── src-tauri/
│   ├── Cargo.toml
│   └── src/
│       └── lib.rs
└── package.json
```

---

## 🚀 Roadmap

Детальный план реализации: [`TODO`](./TODO)

**Основные фазы:**

1. **Базовая функциональность** — Tauri setup, локальное хранение, базовый UI
2. **Интеграция с Runner Agent** — IPC коммуникация, локальный деплой
3. **Генерация кода** — локальная генерация, LLM интеграция
4. **Синхронизация** — опциональная синхронизация с Control Plane
5. **Оптимизация** — производительность, кэширование, UX улучшения

---

## 📚 Связанные Документы

- **[`ARCHITECTURE`](./ARCHITECTURE)** ⭐ — Полная архитектура Axion
  - Раздел "Клиенты" описывает веб и Tauri клиенты
- **[`RUNNER_AGENT`](./RUNNER_AGENT)** ⭐ — Архитектура Axion Runner Agent
  - Раздел "Локальная интеграция" описывает IPC коммуникацию
- **[`TECHNICAL_DETAILS`](./TECHNICAL_DETAILS)** — Технические детали
  - Раздел "Локальный режим" описывает локальную работу

---

## Ключевые выводы

**Tauri Client — это уникальный desktop клиент для Axion с:**

1. **Локальным режимом** ⭐ — **главная фича** — работа без установки сервера, Runner Agent работает локально на вашем компьютере
2. **Нативной интеграцией** — прямое общение с локальным Runner Agent через IPC/Unix Sockets
3. **Высокой производительностью** — нативные компоненты на Rust, быстрый UI на React
4. **Локальным кэшированием** — быстрый доступ к проектам через SQLite
5. **Интеграцией с Control Plane** — использование SaaS для генерации кода, хранения проектов, управления
6. **Гибкостью** — локальная разработка + опциональная синхронизация с Control Plane для production

**Технологический стек:**

- **Tauri v2** — нативное desktop приложение
- **Rust** — backend для производительности и безопасности
- **React + TypeScript** — знакомый UI для разработчиков
- **SQLite** — локальное хранение проектов
- **IPC/Unix Sockets** — нативная коммуникация с Runner Agent
- **Опциональная синхронизация** — гибкая работа с Control Plane

**Архитектурные преимущества:**

- ✅ **Работа без сервера** ⭐ — главная фича: не нужно настраивать SSH, подключать удаленные серверы для разработки
- ✅ **Локальный Runner Agent** — работает на вашем компьютере, управляет локальным Docker Swarm
- ✅ **Мгновенная реакция** — нативная IPC коммуникация с локальным Runner Agent
- ✅ **Быстрый доступ** — локальное кэширование проектов для мгновенной работы
- ✅ **Гибкость** — выбор режима работы (локальный/гибридный/облачный)
- ✅ **Производительность** — нативные компоненты на Rust
- ✅ **Единый UI** — тот же React Flow редактор, что и в веб-версии
- ✅ **Интеграция с SaaS** — использование Control Plane для генерации кода и управления проектами

---

_Этот документ описывает архитектуру Axion Tauri Client — нативного desktop приложения с локальным режимом и нативной интеграцией с Runner Agent._
