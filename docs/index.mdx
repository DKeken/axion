---
title: "Axion Documentation"
description: "Полная документация Axion Meta-Framework — мета-фреймворка нового поколения, где React Flow граф является единственным источником истины для всей архитектуры приложения."
order: 1
---

# Axion Documentation

Добро пожаловать в документацию **Axion Meta-Framework** — мета-фреймворка нового поколения, где React Flow граф является единственным источником истины для всей архитектуры приложения.

## Быстрый старт

Если вы впервые знакомитесь с Axion, рекомендуем начать с этих документов:

1. **[Архитектура](./ARCHITECTURE)** — полный обзор системы и ключевых концепций
2. **[Технические детали](./TECHNICAL_DETAILS)** — детали реализации и технические решения
3. **[Templates](./templates/README)** — система шаблонов для генерации кода

## Структура документации

### Основные разделы

#### Архитектура

**[Архитектура](./ARCHITECTURE)** — полная архитектура Axion Meta-Framework:

- Общая структура и компоненты
- Полный флоу от регистрации до деплоя
- Project Isolation и Blueprint System
- Схемы БД и API endpoints
- Версионирование и монетизация

#### Технические детали

**[Технические детали](./TECHNICAL_DETAILS)** — ключевые технические детали реализации:

- Изоляция контуров между проектами
- Деплой через Axion Runner Agent
- Управление SSH и учет серверов
- Расчет системных требований
- Архитектура генерируемых сервисов vs SaaS платформы

#### Контракты и валидация

**[Контракты и валидация](./CONTRACTS_AND_VALIDATION)** — система контрактов и валидации:

- Protobuf контракты для межсервисного общения
- Автоматическая генерация контрактов из графа
- Validation Pipeline (6 уровней валидации)
- Паттерны отказоустойчивости

#### Runner Agent

**[Runner Agent](./RUNNER_AGENT)** — архитектура Axion Runner Agent:

- Rust агент для всех Linux
- Автоматическая установка
- gRPC + Kafka для типобезопасного общения
- Docker Swarm для оркестрации
- Автообновление с rollback

#### Tauri Client

**[Tauri Client](./TAURI_CLIENT)** — архитектура Axion Tauri Desktop Client:

- Нативное desktop приложение
- Локальный режим работы
- Нативная интеграция с Runner Agent
- Локальное кэширование

### Шаблоны и компоненты

#### Templates

**[Templates](./templates/README)** — единая система шаблонов для генерации кода:

- Blueprints — описания типов сервисов
- Components — переиспользуемые компоненты кода
- Rules — общие правила и паттерны
- Examples — примеры готовых сервисов

### Справочная информация

#### Сравнение

**[Сравнение](./COMPARISON)** — сравнение Axion с CapRover и Convex:

- Уникальные преимущества Axion
- Анализ недостающих функций
- Рекомендации по приоритетам

#### TODO

**[TODO](./TODO)** — полный список задач для реализации:

- 17 фаз разработки
- Приоритеты и статусы
- Progress tracking
- Sprint планирование

---

## 📖 Структура Документации

### Основные Документы

Эти документы описывают общую архитектуру и ключевые концепции:

- **[`ARCHITECTURE`](./ARCHITECTURE)** ⭐ — Полная архитектура системы
  - Общая структура и компоненты
  - Полный флоу от регистрации до деплоя
  - Project Isolation и Blueprint System
  - Схемы БД и API endpoints
  - Версионирование и монетизация

- **[`TECHNICAL_DETAILS`](./TECHNICAL_DETAILS)** — Ключевые технические детали реализации
  - Изоляция контуров между проектами
  - **Деплой через Axion Runner Agent** (WebSocket вместо прямого SSH)
  - Управление SSH и учет серверов
  - Расчет системных требований
  - Архитектура генерируемых сервисов vs SaaS платформы
  - Система сбора и отображения метрик (через Runner Agent)

- **[`CONTRACTS_AND_VALIDATION`](./CONTRACTS_AND_VALIDATION)** — Система контрактов и валидации
  - Protobuf контракты для межсервисного общения
  - Автоматическая генерация контрактов из графа
  - Validation Pipeline (6 уровней валидации)
  - Паттерны отказоустойчивости

- **[`COMPARISON`](./COMPARISON)** — Сравнение с CapRover и Convex
  - Детальное сравнение функций
  - Уникальные преимущества Axion
  - Анализ недостающих функций
  - Рекомендации по приоритетам

- **[`RUNNER_AGENT`](./RUNNER_AGENT)** ⭐ — Архитектура Axion Runner Agent

  **Критически важный компонент для деплоя:**
  - **Rust агент** — один статический бинарник для всех Linux (Ubuntu, Debian, CentOS, etc.)
  - **Автоматическая установка** — устанавливается при добавлении сервера через SSH + BullMQ
  - **gRPC + Kafka** для типобезопасного общения с Control Plane (Protobuf)
  - **Docker Swarm** для продвинутой оркестрации (rolling updates, health checks, scaling)
  - **Автообновление** с проверкой целостности (SHA256) и rollback механизмом
  - Автоматический сбор телеметрии и метрик
  - Автономная работа при проблемах с Control Plane
  - Интегрирован в процесс деплоя (см. [`ARCHITECTURE`](./ARCHITECTURE#phase-5-deployment-с-axion-runner-agent))
  - **Локальная интеграция** с Tauri Client через IPC/Unix Sockets (см. [`TAURI_CLIENT`](./TAURI_CLIENT))

- **[`TAURI_CLIENT`](./TAURI_CLIENT)** ⭐ — Архитектура Axion Tauri Desktop Client

  **Уникальный desktop клиент с локальным режимом:**
  - **Tauri v2** — нативное desktop приложение (Windows, macOS, Linux)
  - **Локальный режим** — **уникальная фича** — работа без установки сервера, Runner Agent работает локально на вашем компьютере
  - **Нативная интеграция с Runner Agent** — прямое общение через IPC/Unix sockets (оба на Rust)
  - **Высокая производительность** — нативные компоненты на Rust, быстрый UI на React
  - **Локальное кэширование** — быстрый доступ к проектам через SQLite
  - **Интеграция с Control Plane** — использование SaaS для генерации кода, хранения проектов, управления
  - **Единый UI** — тот же React Flow редактор, что и в веб-версии
  - Идеально для локальной разработки без необходимости настраивать серверы, быстрого прототипирования и экспериментов

### Templates ⭐

**Единая система шаблонов для генерации кода** — содержит все необходимое: описания типов сервисов, переиспользуемые компоненты, правила и примеры.

- **[`templates/README`](./templates/README)** ⭐ — Обзор системы templates
  - Концепция и структура
  - Как работает генерация кода

- **[`templates/blueprints/`](./templates/blueprints/)** — Описания типов сервисов + шаблоны
  - `service/` ⭐ — единый blueprint для всех генерируемых сервисов

- **[`templates/components/`](./templates/components/)** — Переиспользуемые компоненты кода
  - `main-entry` — точка входа
  - `rabbitmq-server` — RabbitMQ сервер
  - `health-check` — health check endpoint
  - И другие компоненты

- **[`templates/rules/`](./templates/rules/)** — Общие правила и паттерны
  - `contracts-schema` — правила Protobuf контрактов
  - `validation-system` — система валидации (6 уровней)
  - `resilience-patterns` — паттерны отказоустойчивости

- **[`templates/examples/`](./templates/examples/)** — Примеры готовых сервисов
  - Полные примеры для понимания структуры

- **[`templates/TEMPLATE_VARIABLES`](./templates/TEMPLATE_VARIABLES)** — Справочник переменных
  - Все переменные которые подставляются в templates

- **[`templates/TYPES_AND_CONSTANTS`](./templates/TYPES_AND_CONSTANTS)** ⭐ — Типы, енамы и константы
  - Централизованные типы для всей системы
  - Енамы для blueprints, validation, resilience patterns
  - Константы и helper функции

- **[`templates/DATABASE_NODES`](./templates/DATABASE_NODES)** ⭐ — Database Nodes в React Flow
  - Визуальный выбор БД и ORM (Drizzle) через ноды
  - Связывание сервисов с БД через edges (как в n8n)
  - Генерация кода в зависимости от выбранного ORM
  - Переключение между ORM по клику

- **[`templates/EXTENSIBILITY`](./templates/EXTENSIBILITY)** ⭐ — Расширяемость системы
  - Как добавлять новые ORM и blueprints
  - Упрощение кода сервисов через абстракции
  - Factory patterns для компонентов
  - Модульная структура templates

---

## 🚀 Быстрый Старт

1. **Начните с [`ARCHITECTURE`](./ARCHITECTURE)** — главный документ с полной архитектурой
2. **Изучите специфичные детали:**
   - Технические детали → [`TECHNICAL_DETAILS`](./TECHNICAL_DETAILS)
   - Runner Agent → [`RUNNER_AGENT`](./RUNNER_AGENT) ⭐
   - Tauri Desktop Client → [`TAURI_CLIENT`](./TAURI_CLIENT) ⭐
   - Контракты и валидация → [`CONTRACTS_AND_VALIDATION`](./CONTRACTS_AND_VALIDATION)
3. **Работа с templates:**
   - [`templates/README`](./templates/README) — обзор системы templates (включает процесс генерации)
   - [`templates/examples/`](./templates/examples/) — примеры готовых сервисов

---

## Принципы документации

- **Один источник истины** — [`ARCHITECTURE`](./ARCHITECTURE) содержит основную информацию
- **Специфичные детали** — в отдельных документах для удобства навигации
- **Ссылки между документами** — для легкой навигации и понимания связей

---

## Ключевые концепции

### Axion Meta-Framework

Axion — это мета-фреймворк, где React Flow граф является единственным источником истины для всей архитектуры приложения.

| Концепция                    | Описание                                    | Результат                                                                |
| ---------------------------- | ------------------------------------------- | ------------------------------------------------------------------------ |
| **Graph as Source of Truth** | React Flow граф = архитектура = код         | Визуальное проектирование автоматически превращается в код               |
| **Blueprint System**         | Готовые шаблоны сервисов (как в UE5)        | Пользователь выбирает тип, система генерит код, LLM только бизнес-логику |
| **Project Isolation**        | Каждый проект изолирован через `project_id` | Полная изоляция на уровне БД, сети, ресурсов и кода                      |
| **LLM Only Logic**           | LLM генерит только бизнес-логику            | Инфраструктура, контракты, схемы генерируются автоматически              |
| **Visual-First**             | Визуальное проектирование первично          | Всё начинается с графа, код генерируется из него                         |
| **Множественные клиенты**    | Веб и desktop приложения                    | Единый React Flow редактор в обоих клиентах                              |

### Клиенты

| Клиент                   | Платформа                 | Особенности                                                        |
| ------------------------ | ------------------------- | ------------------------------------------------------------------ |
| **Веб-клиент**           | Next.js 15 + React 19 RSC | Облачная работа, совместная работа, production деплой              |
| **Tauri Desktop клиент** | Tauri v2 (Rust + React)   | Локальный режим, нативная интеграция с Runner Agent, офлайн работа |

### Различие: SaaS vs Генерируемые Сервисы

| Аспект                | SaaS Платформа                | Генерируемые Сервисы     |
| --------------------- | ----------------------------- | ------------------------ |
| **Фреймворк**         | NestJS Microservices          | ElysiaJS/Hono            |
| **Назначение**        | Надежность и масштабируемость | Простота и быстрый старт |
| **Service Discovery** | Redis                         | Не требуется             |
| **Event Bus**         | Kafka                         | RabbitMQ                 |
| **База данных**       | Multiple PostgreSQL           | Одна БД на сервис        |

Подробнее: [ARCHITECTURE](./ARCHITECTURE#различие-saas-vs-генерируемые-сервисы)

---

## TODO

- **[`TODO`](./TODO)** ⭐ — Полный список задач для реализации
  - 17 фаз разработки
  - Приоритеты и статусы
  - Progress tracking
  - Sprint планирование

## 🔗 Полезные Ссылки

- **[`ARCHITECTURE`](./ARCHITECTURE)** — начните отсюда
- **[`TECHNICAL_DETAILS`](./TECHNICAL_DETAILS)** — технические детали
- **[`TODO`](./TODO)** ⭐ — TODO для реализации
- **[`templates/`](./templates/)** — шаблоны для генерации кода

---

_Все документы актуальны и синхронизированы с [`ARCHITECTURE`](./ARCHITECTURE)_
