---
title: "Axion Documentation"
description: "Документация Axion Meta-Framework — мета-фреймворка, где React Flow граф является единственным источником истины для архитектуры приложения."
order: 1
---

# Axion Documentation

**Axion Meta-Framework** — мета-фреймворк нового поколения, где React Flow граф является единственным источником истины для всей архитектуры приложения. Визуальное проектирование автоматически превращается в код.

## Что такое Axion?

Axion — это платформа для визуальной разработки микросервисных приложений, которая позволяет создавать, генерировать и деплоить сложные системы без написания шаблонного кода. Вместо написания кода вручную, вы проектируете архитектуру в визуальном редакторе (React Flow), а система автоматически генерирует весь необходимый код, инфраструктуру и контракты.

### Основная идея

**Проблема:** Разработка микросервисов требует много шаблонного кода — настройка инфраструктуры, контракты между сервисами, схемы БД, деплой-конфигурации. Это замедляет разработку и отвлекает от бизнес-логики.

**Решение Axion:** Визуальный граф архитектуры → автоматическая генерация всего шаблонного кода → LLM генерит только бизнес-логику → автоматический деплой на ваши серверы.

## Как это работает?

### Workflow от идеи до production

1. **Проектирование** — Создаете визуальный граф в React Flow редакторе:
   - Перетаскиваете ноды сервисов (CRUD, Auth, Payment и т.д.)
   - Настраиваете через UI формы (без промптов для LLM)
   - Связываете сервисы через edges (RabbitMQ, HTTP, Database)

2. **Генерация кода** — Система автоматически:
   - Генерирует структуру проекта из Blueprint шаблонов
   - Создает Protobuf контракты для межсервисного общения
   - Генерирует схемы БД из UI форм
   - LLM генерит только бизнес-логику (остальное автоматически)
   - Валидирует код через 6-уровневую систему валидации

3. **Деплой** — Один клик для деплоя:
   - Подключаете серверы через SSH
   - Axion Runner Agent автоматически устанавливается
   - Система деплоит все сервисы через Docker Swarm
   - Автоматический мониторинг и health checks

### Ключевые возможности

- **Zero Boilerplate** — Не пишете шаблонный код, только бизнес-логику
- **Type-Safe End-to-End** — Все типы генерируются из графа автоматически
- **Visual-First** — Вся архитектура проектируется визуально
- **Blueprint System** — Готовые шаблоны сервисов (как в Unreal Engine)
- **Project Isolation** — Каждый проект полностью изолирован
- **Множественные клиенты** — Веб и Desktop (Tauri) с локальным режимом

## Use Cases

### Для разработчиков

- **Быстрое прототипирование** — Создайте MVP за минуты, а не дни
- **Локальная разработка** — Tauri Desktop клиент с локальным Runner Agent
- **Микросервисная архитектура** — Визуально проектируйте сложные системы
- **Автоматизация рутины** — Не тратьте время на настройку инфраструктуры

### Для команд

- **Совместная работа** — Real-time синхронизация графов в веб-клиенте
- **Версионирование** — Полная история изменений архитектуры
- **Стандартизация** — Единые Blueprint шаблоны для всей команды
- **Документация** — Граф = живая документация архитектуры

### Для бизнеса

- **Монетизация** — Встроенная система подписок и биллинга
- **Масштабируемость** — От одного сервера до глобальных кластеров
- **Изоляция** — Каждый проект изолирован на всех уровнях
- **Контроль** — Полный контроль над инфраструктурой и кодом

## 🚀 Быстрый старт

1. **[Архитектура](./ARCHITECTURE)** ⭐ — полный обзор системы и ключевых концепций
2. **[Технические детали](./TECHNICAL_DETAILS)** — детали реализации
3. **[Templates](./templates/README)** — система шаблонов для генерации кода

## 📖 Основные документы

- **[`ARCHITECTURE`](./ARCHITECTURE)** ⭐ — Полная архитектура системы
- **[`TECHNICAL_DETAILS`](./TECHNICAL_DETAILS)** — Технические детали реализации
- **[`RUNNER_AGENT`](./RUNNER_AGENT)** ⭐ — Rust агент для деплоя (Docker Swarm, gRPC + Kafka)
- **[`TAURI_CLIENT`](./TAURI_CLIENT)** ⭐ — Desktop клиент с локальным режимом
- **[`CONTRACTS_AND_VALIDATION`](./CONTRACTS_AND_VALIDATION)** — Protobuf контракты и валидация
- **[`templates/`](./templates/)** ⭐ — Система шаблонов для генерации кода
- **[`TODO`](./TODO)** — Список задач для реализации

## Ключевые концепции

| Концепция                    | Описание                                    | Результат                                                                |
| ---------------------------- | ------------------------------------------- | ------------------------------------------------------------------------ |
| **Graph as Source of Truth** | React Flow граф = архитектура = код         | Визуальное проектирование автоматически превращается в код               |
| **Blueprint System**         | Готовые шаблоны сервисов (как в UE5)        | Пользователь выбирает тип, система генерит код, LLM только бизнес-логику |
| **Project Isolation**        | Каждый проект изолирован через `project_id` | Полная изоляция на уровне БД, сети, ресурсов и кода                      |
| **LLM Only Logic**           | LLM генерит только бизнес-логику            | Инфраструктура, контракты, схемы генерируются автоматически              |
| **Visual-First**             | Визуальное проектирование первично          | Всё начинается с графа, код генерируется из него                         |
| **Множественные клиенты**    | Веб и desktop приложения                    | Единый React Flow редактор в обоих клиентах                              |

### Клиенты

| Клиент                   | Платформа                 | Особенности                                                        |
| ------------------------ | ------------------------- | ------------------------------------------------------------------ |
| **Веб-клиент**           | Next.js 15 + React 19 RSC | Облачная работа, совместная работа, production деплой              |
| **Tauri Desktop клиент** | Tauri v2 (Rust + React)   | Локальный режим, нативная интеграция с Runner Agent, офлайн работа |

### SaaS vs Генерируемые Сервисы

| Аспект                | SaaS Платформа                | Генерируемые Сервисы     |
| --------------------- | ----------------------------- | ------------------------ |
| **Фреймворк**         | NestJS Microservices          | ElysiaJS/Hono            |
| **Назначение**        | Надежность и масштабируемость | Простота и быстрый старт |
| **Service Discovery** | Redis                         | Не требуется             |
| **Event Bus**         | Kafka                         | RabbitMQ                 |
| **База данных**       | Multiple PostgreSQL           | Одна БД на сервис        |

## Пример использования

### Создание E-commerce системы

**Шаг 1: Проектирование**

```
1. Создаете проект в веб-клиенте
2. Открываете React Flow редактор
3. Перетаскиваете ноды:
   - Auth Service (Blueprint: Auth)
   - Product Service (Blueprint: CRUD)
   - Order Service (Blueprint: CRUD)
   - Payment Service (Blueprint: Payment)
4. Настраиваете через UI:
   - Product Service: Entity "Product" с полями (name, price, stock)
   - Order Service: Entity "Order" с полями (userId, items, total)
5. Связываете через edges:
   - Auth → Order (HTTP)
   - Product → Order (RabbitMQ)
   - Order → Payment (RabbitMQ)
```

**Шаг 2: Генерация**

```
1. Нажимаете "Generate All"
2. Система автоматически:
   - Генерирует структуру всех сервисов
   - Создает Protobuf контракты для RabbitMQ
   - Генерирует схемы БД для Product и Order
   - LLM генерит бизнес-логику (создание заказа, обработка платежа)
3. Валидация проходит успешно ✅
```

**Шаг 3: Деплой**

```
1. Подключаете сервер через SSH
2. Выбираете проект и нажимаете "Deploy"
3. Axion Runner Agent:
   - Устанавливается автоматически
   - Создает Docker Swarm кластер
   - Деплоит все сервисы
   - Настраивает сети и health checks
4. Система готова к работе! 🚀
```

**Результат:** Полноценная E-commerce система с микросервисной архитектурой, типобезопасными контрактами и автоматическим деплоем — без написания шаблонного кода.

## Ценностное предложение

### Для кого Axion?

- **Стартапы** — Быстрое создание MVP без найма DevOps инженеров
- **Разработчики** — Фокус на бизнес-логике, а не на инфраструктуре
- **Команды** — Стандартизация и ускорение разработки
- **Предприятия** — Масштабируемая платформа с полным контролем

### Уникальные преимущества

1. **Визуальное проектирование** — Единственная платформа, где граф = код
2. **Zero Boilerplate** — LLM генерит только бизнес-логику
3. **Type-Safe End-to-End** — Все типы из графа, без ручной типизации
4. **Локальный режим** — Tauri Desktop клиент для офлайн разработки
5. **Автоматический деплой** — Rust Runner Agent для всех Linux систем
6. **Project Isolation** — Полная изоляция на всех уровнях

### Отличия от конкурентов

| Функция                   | Axion            | CapRover | Convex | Vercel |
| ------------------------- | ---------------- | -------- | ------ | ------ |
| Визуальное проектирование | ✅ React Flow    | ❌       | ❌     | ❌     |
| Генерация кода из графа   | ✅               | ❌       | ❌     | ❌     |
| Микросервисы              | ✅               | ⚠️       | ❌     | ❌     |
| Собственные серверы       | ✅               | ✅       | ❌     | ❌     |
| Локальный режим           | ✅ Tauri Desktop | ❌       | ❌     | ❌     |
| Type-Safe контракты       | ✅ Protobuf      | ❌       | ⚠️     | ❌     |

---

_Все документы актуальны и синхронизированы с [`ARCHITECTURE`](./ARCHITECTURE)_
