---
title: "CI/CD Pipeline"
description: "Continuous Integration и Continuous Deployment для Axion Stack"
order: 13
---

# CI/CD Pipeline

Axion Stack использует GitHub Actions для автоматизации CI/CD процессов.

## 📋 CI Pipeline

### Основные этапы

1. **Lint & Type Check** - проверка кода на ошибки
2. **Test** - запуск тестов (unit, integration, contract)
3. **Build** - сборка всех пакетов

### GitHub Actions Workflow

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-typecheck:
    # Линтинг и проверка типов
  test:
    # Запуск тестов с инфраструктурой (PostgreSQL, Redis, Kafka)
  build:
    # Сборка всех пакетов
```

### Кэширование

CI использует кэширование для ускорения:

- **Bun dependencies** - кэш `~/.bun/install/cache`
- **Node modules** - кэш `node_modules`
- **Turborepo cache** - кэш `.turbo`

### Тестовая инфраструктура

CI автоматически поднимает необходимые сервисы:

- **PostgreSQL** - для тестов баз данных
- **Redis** - для BullMQ тестов
- **Kafka** - для интеграционных тестов

## 🔧 Локальная проверка

Перед push можно запустить те же проверки локально:

```bash
# Линтинг
bun run lint

# Проверка типов
bun run type-check

# Тесты
bun run test

# Сборка
bun run build
```

## 📝 Best Practices

1. **Всегда обновляйте bun.lock** - используйте `bun install` для обновления зависимостей
2. **Запускайте тесты локально** - перед push убедитесь, что все тесты проходят
3. **Проверяйте типы** - `bun run type-check` должен проходить без ошибок
4. **Не коммитьте сломанный код** - CI будет проверять каждый PR

## 🔗 См. также

- [Development Setup](./DEVELOPMENT_SETUP.md) - настройка окружения
- [E2E Testing](./E2E_TESTING.mdx) - тестирование
- [Architecture](./ARCHITECTURE.mdx) - архитектура системы
