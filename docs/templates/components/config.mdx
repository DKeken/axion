---
title: "Component: Configuration"
description: "Конфигурация сервиса через environment variables."
order: 0
---

# Component: Configuration

## Описание

Конфигурация сервиса через environment variables.

**Используется в:** всех сервисах

## Конфигурация

- `{SERVICE_NAME}` — имя сервиса
- `{REQUIRED_ENV_VARS}` — список обязательных переменных окружения

## Код

```typescript
// src/config/index.ts
export const config = {
  service: {
    name: "{SERVICE_NAME}",
    port: parseInt(process.env.PORT || "3000", 10),
  },
  rpc: {
    // Service-to-service calls inside Docker/Swarm go by DNS name:
    //   http://<service-name>:<port>
    // Override per-service via env:
    //   <SERVICE_NAME>_URL, e.g. GRAPH_SERVICE_URL="http://graph-service:3000"
    pathPrefix: "{RPC_PATH_PREFIX}",
  },
  database: {
    url: process.env.DATABASE_URL!,
  },
  validate() {
    const required = [{ REQUIRED_ENV_VARS }];
    const missing = required.filter((key) => !process.env[key]);
    if (missing.length > 0) {
      throw new Error(
        `Missing required environment variables: ${missing.join(", ")}`
      );
    }
  },
};

config.validate();
```

## Checklist

- [ ] Все переменные окружения определены
- [ ] Валидация работает
- [ ] Значения по умолчанию установлены

---

_Конфигурация генерируется автоматически из шаблона._
