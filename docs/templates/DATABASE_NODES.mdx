---
title: "Database Nodes"
description: "База данных как отдельная нода в React Flow графе: визуальный выбор БД и ORM, связывание с сервисами."
order: 0
---

# Database Nodes

## Концепция

База данных — это отдельная **нода в React Flow графе**, которую можно:

- Перетаскивать на canvas
- Выбирать тип БД (PostgreSQL, MySQL)
- Выбирать ORM (**Текущая реализация:** Drizzle)
- Связывать с сервисами через edges (как в n8n)

<Callout type="info" title="Расширяемость">
  Система спроектирована для добавления новых ORM и типов БД. См.
  [EXTENSIBILITY](./EXTENSIBILITY) для инструкций по добавлению новых ORM
  (TypeORM и т.д.)
</Callout>

## Как это работает

### 1. Создание Database Node

```
React Flow → Drag "Database" ноду на canvas
↓
Модальное окно:
┌─────────────────────────────────────┐
│  Database Configuration            │
├─────────────────────────────────────┤
│  Database Type:                     │
│  ○ PostgreSQL                       │
│  ● MySQL                            │
│                                     │
│  ORM:                               │
│  ● Drizzle ORM                      │
│                                     │
│  Connection Name: [postgres-db]     │
│  Project: [project-123]             │
└─────────────────────────────────────┘
```

### 2. Структура Database Node

```typescript
interface DatabaseNode {
  id: string; // node_id
  type: "database";
  data: {
    databaseType: "postgresql" | "mysql"; // Расширяемо: можно добавить MongoDB, Redis и т.д.
    orm: "drizzle"; // Текущая реализация: только Drizzle. Расширяемо (см. [`EXTENSIBILITY`](../EXTENSIBILITY))
    connectionName: string; // Уникальное имя в проекте
    projectId: string;
    config: {
      host?: string; // Для локальной разработки
      port?: number;
      database?: string;
      // Или использовать переменные окружения
    };
  };
  position: { x: number; y: number };
}
```

### 3. Связывание с Сервисами

```
Service Node → Edge → Database Node
     ↓
  Тип edge: "database"
     ↓
  Сервис получает доступ к БД
```

**Визуально:**

```
┌─────────────┐      database      ┌─────────────┐
│   Service   │ ──────────────────→ │  Database   │
│  (Product)  │                     │ (PostgreSQL)│
└─────────────┘                     └─────────────┘
```

### 4. Генерация кода

| Шаг                    | Действие                               | Результат                         |
| ---------------------- | -------------------------------------- | --------------------------------- |
| **1. Поиск edges**     | Находит все database edges для сервиса | Список связанных database nodes   |
| **2. Определение ORM** | Определяет ORM из database node        | ORM тип (drizzle, typeorm и т.д.) |
| **3. Генерация кода**  | Генерит соответствующий код            | Файлы для выбранного ORM          |

| ORM                   | Используемые компоненты                                                     |
| --------------------- | --------------------------------------------------------------------------- |
| **Drizzle**           | `components/drizzle/schema.mdx`, `components/drizzle/repository.mdx`        |
| **TypeORM** (будущее) | `components/typeorm/schema.mdx`, `components/typeorm/repository.mdx`        |
| **Другие ORM**        | Автоматически из `components/{orm}/` (см. [EXTENSIBILITY](./EXTENSIBILITY)) |

## Реализация

### Graph Structure

```typescript
import {
  DatabaseNodeData,
  ORMType,
  DatabaseType,
} from "./TYPES_AND_CONSTANTS";

interface AxionGraph {
  nodes: Array<
    | ServiceNode
    | DatabaseNode
    | EdgeNode // Traefik edge routing (не отдельный сервис)
    | ...
  >;
  edges: Array<{
    id: string;
    source: string; // node_id сервиса
    target: string; // node_id БД
    type: "database";
    data?: {
      // Дополнительные настройки связи
      tables?: string[]; // Какие таблицы использует сервис
    };
  }>;
}

interface DatabaseNode {
  id: string;
  type: "database";
  data: DatabaseNodeData;
  position: { x: number; y: number };
}
```

### Code Generation Logic

```typescript
import {
  ORMType,
  getORMComponents,
  ComponentType,
} from "./TYPES_AND_CONSTANTS";

async function generateServiceCode(
  serviceNode: ServiceNode,
  graph: AxionGraph
) {
  // 1. Находим все database edges для этого сервиса
  const dbEdges = graph.edges.filter(
    (e) => e.source === serviceNode.id && e.type === "database"
  );

  // 2. Для каждой БД определяем ORM
  const databases = dbEdges.map((edge) => {
    const dbNode = graph.nodes.find(
      (n) => n.id === edge.target && n.type === "database"
    ) as DatabaseNode;
    return {
      node: dbNode,
      orm: dbNode.data.orm as ORMType,
      connectionName: dbNode.data.connectionName,
    };
  });

  // 3. Генерируем код в зависимости от ORM
  for (const db of databases) {
    // Получаем компоненты для выбранного ORM
    const ormComponents: ComponentType[] = getORMComponents(db.orm);

    // Генерируем код для каждого компонента
    for (const component of ormComponents) {
      await generateComponentCode(serviceNode, db, component);
    }
  }
}
```

## Templates для разных ORM

### Структура Components

```
components/
├── database/
│   └── base/
│       └── connection.mdx      # Абстракция подключения
├── drizzle/
│   ├── schema.mdx
│   ├── repository.mdx
│   └── connection.ts.mdx
└── (другие ORM можно добавить, см. [`EXTENSIBILITY`](../EXTENSIBILITY))
```

### Абстракция ORM

```typescript
// components/database/base/connection.mdx
// Абстракция для подключения к БД

import { {ORM_CONNECTION} } from "./{ORM}/connection";

export class DatabaseConnection {
  private connection: {ORM_TYPE};

  constructor(connectionName: string) {
    this.connection = {ORM_CONNECTION}({
      url: process.env[`DATABASE_URL_${connectionName.toUpperCase()}`],
    });
  }

  async connect() {
    return await this.connection.connect();
  }

  async disconnect() {
    return await this.connection.disconnect();
  }

  getClient() {
    return this.connection;
  }
}
```

## UI/UX в React Flow

| Элемент           | Описание                                                    | Визуализация                                                 |
| ----------------- | ----------------------------------------------------------- | ------------------------------------------------------------ |
| **Database Node** | Отображает тип БД, ORM, connection name, статус подключения | Иконка БД, цвет по типу                                      |
| **Edge Styling**  | Database edges — зеленые пунктирные линии                   | Визуальное отличие от других edges                           |
| **Context Menu**  | Действия с database node                                    | Change ORM, View Schema, Generate Migration, Test Connection |

## Миграция между ORM

| Шаг                      | Действие                                | Результат            |
| ------------------------ | --------------------------------------- | -------------------- |
| **1. Чтение схемы**      | Читает текущую схему из БД              | Текущая структура БД |
| **2. Конвертация**       | Конвертирует схему в новый ORM формат   | Схема для нового ORM |
| **3. Генерация кода**    | Генерирует новый код для выбранного ORM | Файлы для нового ORM |
| **4. Создание миграции** | Создает миграцию для обновления БД      | SQL миграция         |
| **5. Обновление node**   | Обновляет database node в графе         | Новый ORM в графе    |

<Callout type="info" title="Автоматическая миграция">
  При смене ORM система автоматически выполняет все шаги миграции, включая
  backup данных.
</Callout>

## Типы и константы

```typescript
// TYPES_AND_CONSTANTS.md

export enum DatabaseType {
  POSTGRESQL = "postgresql",
  MYSQL = "mysql",
}

export enum ORMType {
  DRIZZLE = "drizzle",
  // Для расширения: можно добавлять новые ORM
  // TYPEORM = "typeorm",
  // См. EXTENSIBILITY.md
}

export interface DatabaseNodeData {
  databaseType: DatabaseType;
  orm: ORMType;
  connectionName: string;
  projectId: string;
  config?: {
    host?: string;
    port?: number;
    database?: string;
  };
}

export const ORM_TEMPLATES: Record<ORMType, ComponentType[]> = {
  [ORMType.DRIZZLE]: [
    ComponentType.DRIZZLE_SCHEMA,
    ComponentType.DRIZZLE_REPOSITORY,
  ],
  // Для расширения: добавляй новые ORM здесь
};

export function getORMComponents(orm: ORMType): ComponentType[] {
  return ORM_TEMPLATES[orm] || [];
}
```

## Преимущества

| Преимущество          | Описание                         | Результат                          |
| --------------------- | -------------------------------- | ---------------------------------- |
| **Визуальность**      | Видно какие БД использует сервис | Понятная архитектура в графе       |
| **Гибкость**          | Легко менять ORM по клику        | Быстрое переключение между ORM     |
| **Масштабируемость**  | Можно добавить новые ORM         | Расширяемость системы              |
| **Переиспользование** | Одна БД для нескольких сервисов  | Эффективное использование ресурсов |
| **Типобезопасность**  | Все связи типизированы           | End-to-end типобезопасность        |

## 🔗 Связанные Документы

- **[`TYPES_AND_CONSTANTS.mdx`](./TYPES_AND_CONSTANTS)** — Типы для database nodes
- **[`README.mdx`](./README)** — Процесс генерации кода с БД
- **[`components/database/`](./components/database/)** — Templates для разных ORM

---

_Database Nodes делают выбор БД визуальным и гибким, как в n8n._
