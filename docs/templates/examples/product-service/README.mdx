---
title: "Example: Product Service (CRUD)"
description: "Полный пример CRUD сервиса для продуктов. Показывает результат генерации из templates."
order: 1
---

# Example: Product Service (CRUD)

## Описание

Полный пример CRUD сервиса для продуктов. Показывает результат генерации из templates.

<Callout type="info" title="Пример">
  Этот пример демонстрирует полный CRUD сервис, сгенерированный из templates.
  Используйте его как референс при создании собственных сервисов.
</Callout>

## Конфигурация

- Entity: `Product`
- Fields: `name`, `price`, `description`, `category_id`
- Blueprint: `service`
- ORM: `drizzle`

## 📁 Структура

```
apps/_services/product-service/
├── src/
│   ├── main.ts                    # ← main-entry.md
│   ├── messaging/server.ts        # ← rabbitmq-server.md
│   ├── database/
│   │   ├── connection.ts          # ← database/base/connection.md
│   │   ├── schema.ts               # ← drizzle/schema.md
│   │   └── repository.ts          # ← drizzle/repository.md
│   ├── modules/product/
│   │   └── product.service.ts     # ← LLM сгенерировал
│   └── health/health.check.ts     # ← health-check.md
└── proto/product.proto            # ← Автоматически
```

## 📄 Файлы

В этой папке находятся примеры сгенерированного кода:

- **`main.ts`** — точка входа сервиса (сгенерировано из `main-entry.md`)
- **`product.service.ts`** — бизнес-логика (сгенерировано LLM)
- **`schema.ts`** — схема базы данных (сгенерировано из `drizzle/schema.md`)

Все эти файлы демонстрируют результат работы системы генерации кода из графа.

---

_Пример показывает результат генерации из templates._
