---
title: "Templates"
description: "Единая система шаблонов для генерации кода в Axion: blueprints, компоненты, правила и примеры."
order: 1
---

# Templates

## Концепция

Единая система шаблонов для генерации кода в Axion. Содержит все необходимое: описания типов сервисов (blueprints), переиспользуемые компоненты кода, правила и примеры.

<Callout type="idea" title="Ключевая идея">
  Визуальное проектирование → выбор blueprint → сборка из компонентов → LLM
  генерит только бизнес-логику → готовый код.
</Callout>

## 📁 Структура

```
templates/
├── blueprints/              # Описания типов сервисов + шаблоны
│   └── service/             # Единый blueprint для всех генерируемых сервисов ⭐
│
├── components/              # Переиспользуемые компоненты кода
│   ├── main-entry.mdx        # Точка входа
│   ├── http-rpc-server.mdx   # HTTP RPC router (server-to-server внутри Docker/Swarm)
│   ├── http-rpc-client.mdx   # HTTP RPC client (вызов других сервисов по DNS имени)
│   ├── health-check.mdx      # Health check
│   ├── database/            # Database компоненты
│   │   ├── base/            # Базовые абстракции
│   │   │   └── connection.mdx # Абстракция подключения
│   │   ├── drizzle/         # Drizzle ORM
│   │   │   ├── schema.mdx
│   │   │   ├── repository.mdx
│   │   │   └── connection.ts.mdx
│   │   └── (другие ORM можно добавить, см. [`EXTENSIBILITY`](../EXTENSIBILITY))
│   └── ...
│
├── rules/                   # Общие правила и паттерны
│   ├── contracts-schema.mdx  # Правила Protobuf контрактов
│   ├── validation-system.mdx # Система валидации (6 уровней)
│   └── resilience-patterns.mdx # Паттерны отказоустойчивости
│
├── examples/               # Примеры готовых сервисов
│   └── product-service/     # Полный пример CRUD сервиса
│
├── TEMPLATE_VARIABLES.md    # Справочник переменных
├── TYPES_AND_CONSTANTS.md   # Типы, енамы и константы ⭐
├── DATABASE_NODES.md        # Database Nodes в React Flow ⭐
└── EXTENSIBILITY.md         # Расширяемость системы ⭐
```

## Как это работает

### 1. Blueprints (Описания Типов Сервисов)

Blueprints описывают структуру и правила для различных типов сервисов:

- **[`service/`](./blueprints/service/)** ⭐ — единый blueprint для всех генерируемых сервисов
  - Используется для всех генерируемых сервисов (ElysiaJS/Hono)
  - Минимальные зависимости, быстрый старт
  - БД добавляется через database node (опционально)
  - Автоматическая генерация CRUD операций при наличии БД

> **Примечание:** Edge routing выполняет Traefik (управляется Runner Agent), это не отдельный сервис и не генерируется через templates.

Каждый blueprint содержит:

- `README.mdx` — описание структуры и правил
- `structure.mdx` — какие компоненты использовать
- `service.ts.mdx` — шаблон для LLM (что генерит LLM)
- `handler.ts.mdx` — шаблон handlers (генерируется автоматически)

### 2. Components (Переиспользуемые Компоненты)

Готовые шаблоны кода для переиспользования:

- `main-entry.mdx` — точка входа приложения
- `http-rpc-server.mdx` — HTTP RPC router для межсервисного общения внутри Docker/Swarm
- `http-rpc-client.mdx` — HTTP RPC client для вызова других сервисов по DNS имени
- `health-check.mdx` — health check endpoint
- `database/base/connection.mdx` — абстракция подключения к БД
- `drizzle/` — компоненты для Drizzle ORM
- И другие...

### 3. Rules (Общие Правила)

Правила и паттерны применяемые ко всем сервисам:

- **[`rules/contracts-schema.mdx`](./rules/contracts-schema)** — как создавать Protobuf контракты
- **[`rules/validation-system.mdx`](./rules/validation-system)** — 6 уровней валидации
- **[`rules/resilience-patterns.mdx`](./rules/resilience-patterns)** — retry, circuit breaker, timeout и т.д.

## Процесс генерации

| Шаг                                    | Действие                                                                     | Результат                       |
| -------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------- |
| **1. Выбор Blueprint**                 | Пользователь выбирает тип сервиса в React Flow                               | Выбранный blueprint ID          |
| **2. Загрузка Blueprint**              | Система загружает описание из `templates/blueprints/`                        | Структура Blueprint             |
| **3. Определение Database**            | Анализ database edges, определение ORM из database node                      | ORM тип (Drizzle и т.д.)        |
| **4. Загрузка Structure**              | Загрузка `structure.md`, получение компонентов                               | Список обязательных компонентов |
| **5. Загрузка Компонентов**            | Загрузка templates из `components/` (ORM-специфичные из `components/{orm}/`) | Готовые шаблоны компонентов     |
| **6. Подстановка Переменных**          | Заполнение `{VARIABLES}` из конфигурации                                     | Заполненные шаблоны             |
| **7. Генерация Автоматических Файлов** | Создание файлов без LLM (main.ts, server.ts, schema.ts, proto)               | Базовые файлы сервиса           |
| **8. LLM Генерация**                   | Только для файлов с `LLMGenerationPoint.SERVICE_METHODS`                     | Бизнес-логика от LLM            |
| **9. Сборка**                          | Объединение всех файлов в финальную структуру                                | Полная структура сервиса        |
| **10. Валидация**                      | Проверка соответствия правилам из `rules/`                                   | Статус валидации                |

**Что идет в LLM:**

- Описание blueprint и правила
- Контекст (entity, fields, типы)
- Шаблон с маркером `{LLM_GENERATED_METHODS}`
- Инструкции что генерировать

**LLM возвращает:** Только код методов (без импортов, без класса). Код вставляется в шаблон на место `{LLM_GENERATED_METHODS}`.

Database Nodes: [`DATABASE_NODES.md`](./DATABASE_NODES)

## Принципы

| Принцип               | Описание                                     | Преимущества                                |
| --------------------- | -------------------------------------------- | ------------------------------------------- |
| **Модульность**       | Каждый компонент в отдельном файле           | Легко найти и изменить нужный компонент     |
| **Визуальность**      | Видно весь код который идет в LLM            | Прозрачность процесса генерации             |
| **Редактируемость**   | Легко править шаблоны                        | Быстрое улучшение и кастомизация            |
| **Переиспользование** | Компоненты используются в разных blueprints  | Единообразие, меньше дублирования           |
| **Четкое разделение** | Что автоматически, что LLM, что пользователь | Понятная ответственность каждого компонента |

## Формат файлов

Каждый `.mdx` файл содержит:

```markdown
# Название Компонента

## Описание

Что это за компонент и где используется

## Конфигурация

Какие переменные подставляются: {SERVICE_NAME}, {ENTITY_NAME}, etc.

## Код

\`\`\`typescript
// Весь код компонента
// {variable} - подстановки
\`\`\`

## LLM Инструкции

Что именно генерит LLM (если применимо)

## Checklist

Что проверить после генерации
```

> **См. [`TEMPLATE_VARIABLES.mdx`](./TEMPLATE_VARIABLES)** для полного списка переменных.

## Быстрый старт

### Для Service с БД

| Шаг                  | Действие                            | Параметры                                          |
| -------------------- | ----------------------------------- | -------------------------------------------------- |
| **1. Database Node** | Создай database node в React Flow   | PostgreSQL + Drizzle, Connection Name: `main-db`   |
| **2. Service Node**  | Создай service node                 | Blueprint: `service`, Feature: `Product`           |
| **3. Связь**         | Свяжи Service → Database через edge | Тип edge: `database`                               |
| **4. Генерация**     | Система автоматически генерит код   | Определяет ORM, генерит код, добавляет CRUD методы |

<Callout type="info" title="Расширяемость">
  Другие ORM можно добавить, см. [EXTENSIBILITY](./EXTENSIBILITY)
</Callout>

### Для редактирования templates

| Шаг                           | Действие                                              | Результат                              |
| ----------------------------- | ----------------------------------------------------- | -------------------------------------- |
| **1. Определить Blueprint**   | `blueprintId` из service node (например: `"service"`) | ID blueprint                           |
| **2. Найти Templates**        | `templates/blueprints/{blueprintId}/`                 | Файлы шаблонов                         |
| **3. Просмотреть Компоненты** | `templates/components/`                               | Переиспользуемые части                 |
| **4. Редактировать**          | Отредактировать нужный template                       | Все сервисы используют обновленный код |

| Тип генерации     | Источник                                | Результат                                  |
| ----------------- | --------------------------------------- | ------------------------------------------ |
| **Автоматически** | `components/*.mdx`                      | `src/*.ts`                                 |
| **LLM**           | `blueprints/{blueprint}/service.ts.mdx` | `src/modules/{entity}/{entity}.service.ts` |

## Дополнительные ресурсы

- **[`TEMPLATE_VARIABLES.mdx`](./TEMPLATE_VARIABLES)** — Справочник всех переменных
- **[`TYPES_AND_CONSTANTS.mdx`](./TYPES_AND_CONSTANTS)** ⭐ — Типы, енамы и константы
- **[`DATABASE_NODES.mdx`](./DATABASE_NODES)** ⭐ — Database Nodes в React Flow
- **[`EXTENSIBILITY.mdx`](./EXTENSIBILITY)** ⭐ — Расширяемость системы (добавление ORM, blueprints)
- **[`examples/`](./examples/)** — Примеры готовых сервисов
- **[`rules/`](./rules/)** — Общие правила и паттерны

## Связанные документы

### Основная Архитектура

- **[`../ARCHITECTURE.mdx`](../ARCHITECTURE)** ⭐ — Общая архитектура системы
  - Phase 3: Code Generation описывает как templates используются для генерации кода
  - Blueprint System описывает концепцию blueprints
- **[`../TECHNICAL_DETAILS.mdx`](../TECHNICAL_DETAILS)** — Технические детали реализации
  - Раздел "Упрощенная Архитектура Генерируемых Сервисов" описывает почему легковесные фреймворки
- **[`../RUNNER_AGENT.mdx`](../RUNNER_AGENT)** — Архитектура Runner Agent
  - Сгенерированные сервисы деплоятся через Runner Agent
- **[`../CONTRACTS_AND_VALIDATION.mdx`](../CONTRACTS_AND_VALIDATION)** — Система контрактов и валидации
  - Validation Pipeline (6 уровней) проверяет сгенерированный код
  - Protobuf контракты генерируются автоматически из графа

### Templates Документация

- **[`TEMPLATE_VARIABLES.mdx`](./TEMPLATE_VARIABLES)** — Справочник всех переменных
- **[`TYPES_AND_CONSTANTS.mdx`](./TYPES_AND_CONSTANTS)** ⭐ — Типы, енамы и константы
- **[`DATABASE_NODES.mdx`](./DATABASE_NODES)** ⭐ — Database Nodes в React Flow
- **[`EXTENSIBILITY.mdx`](./EXTENSIBILITY)** ⭐ — Расширяемость системы

---

## Интеграция в общий флоу

**Templates являются частью полного флоу Axion:**

1. **Визуальное проектирование** (React Flow) → [`ARCHITECTURE.mdx`](../ARCHITECTURE#phase-2-building-architecture)
2. **Выбор Blueprint** → этот документ (templates/blueprints/)
3. **Генерация кода** → раздел "Процесс Генерации" выше
4. **Валидация** → [`../CONTRACTS_AND_VALIDATION.mdx`](../CONTRACTS_AND_VALIDATION#-система-валидации)
5. **Деплой через Runner Agent** → [`../RUNNER_AGENT.mdx`](../RUNNER_AGENT)

**Сгенерированный код:**

- Использует легковесные фреймворки (ElysiaJS/Hono) для простоты
- Генерируется в `generated/{project_id}/`
- Деплоится через Runner Agent на серверы клиентов
- Валидируется через Validation Pipeline (6 уровней)

---

_Templates содержат все необходимое для генерации кода: описания, компоненты, правила и примеры. Templates интегрированы в общий флоу Axion от визуального проектирования до деплоя через Runner Agent._
