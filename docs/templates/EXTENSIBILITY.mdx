---
title: "Extensibility"
description: "Axion спроектирован для легкого расширения. Можно добавлять: новые ORM, типы БД, компоненты, blueprints."
order: 1
---

# Extensibility

## Обзор

Axion спроектирован для легкого расширения. Можно добавлять: новые ORM, типы БД, компоненты, blueprints.

<Callout type="idea" title="Модульная архитектура">
  Система построена на принципах модульности и абстракции, что позволяет легко
  добавлять новые компоненты без изменения существующего кода.
</Callout>

## Принципы

| Принцип         | Описание                                                   | Результат                   |
| --------------- | ---------------------------------------------------------- | --------------------------- |
| **Модульность** | Каждый ORM/компонент в отдельной папке                     | Легко найти и изменить      |
| **Абстракция**  | Единые интерфейсы для разных реализаций                    | Консистентность API         |
| **Регистрация** | Новые компоненты регистрируются в `TYPES_AND_CONSTANTS.md` | Централизованное управление |
| **Шаблоны**     | Каждый компонент имеет свой template в `components/`       | Переиспользование           |

## Ключевые улучшения

| Улучшение                          | Описание                                                                                      | Преимущества                             |
| ---------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------- |
| **Database Nodes в React Flow**    | БД стала отдельной нодой, выбор типа БД и ORM через edges                                     | Визуальность, гибкость, масштабируемость |
| **Модульная структура Components** | Разделение: `components/database/base/` (абстракции), `components/drizzle/` (ORM-специфичные) | Легко добавлять новые ORM                |
| **Централизованные типы**          | `TYPES_AND_CONSTANTS.md` содержит все типы, енамы, helper функции                             | Единый источник истины                   |
| **Упрощение кода сервисов**        | Factory patterns автоматически создают компоненты из графа                                    | LLM генерит только бизнес-логику         |
| **Factory Patterns**               | `createDatabase()` и `createMessaging()` автоматически настраивают компоненты                 | Минимум boilerplate                      |

## Добавление нового ORM

| Шаг                                | Действие                          | Файл                                               | Описание                   |
| ---------------------------------- | --------------------------------- | -------------------------------------------------- | -------------------------- |
| **1. Добавить ORM в Enum**         | Добавить новый ORM в `ORMType`    | `templates/TYPES_AND_CONSTANTS.md`                 | Регистрация нового ORM     |
| **2. Добавить Component Types**    | Добавить типы компонентов для ORM | `templates/TYPES_AND_CONSTANTS.md`                 | Определение компонентов    |
| **3. Зарегистрировать компоненты** | Добавить в `ORM_TEMPLATES`        | `templates/TYPES_AND_CONSTANTS.md`                 | Связь ORM с компонентами   |
| **4. Создать компоненты**          | Создать templates для ORM         | `templates/components/{orm}/`                      | Реализация компонентов     |
| **5. Обновить DatabaseConnection** | Добавить поддержку нового ORM     | `templates/components/database/base/connection.md` | Интеграция в базовый класс |

### Шаг 1: Добавить ORM в Enum

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export enum ORMType {
  DRIZZLE = "drizzle",
  TYPEORM = "typeorm", // ← Добавь новый ORM
}
```

### Шаг 2: Добавить Component Types

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export enum ComponentType {
  // ... существующие
  TYPEORM_SCHEMA = "typeorm-schema", // ← Добавь
  TYPEORM_REPOSITORY = "typeorm-repository", // ← Добавь
}
```

### Шаг 3: Зарегистрировать компоненты ORM

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export const ORM_TEMPLATES: Record<ORMType, ComponentType[]> = {
  [ORMType.DRIZZLE]: [
    ComponentType.DRIZZLE_SCHEMA,
    ComponentType.DRIZZLE_REPOSITORY,
  ],
  [ORMType.TYPEORM]: [
    // ← Добавь
    ComponentType.TYPEORM_SCHEMA,
    ComponentType.TYPEORM_REPOSITORY,
  ],
};
```

### Шаг 4: Создать компоненты

**Структура:**

```
templates/components/
├── typeorm/
│   ├── connection.ts.md      # Подключение к БД
│   ├── schema.md              # Schema шаблон
│   └── repository.md          # Repository шаблон
```

**Пример: `typeorm/connection.ts.md`**

````markdown
# 🔌 Component: TypeORM Connection

## Описание

Реализация подключения к БД через TypeORM.

## 📄 Код

```typescript
// src/database/typeorm/connection.ts
import { DataSource } from "typeorm";

export function createTypeORMConnection(databaseUrl: string) {
  return new DataSource({
    type: "postgres",
    url: databaseUrl,
    // ... конфигурация
  });
}

export async function typeormHealthCheck(dataSource: DataSource) {
  await dataSource.query("SELECT 1");
}

export async function typeormDisconnect(dataSource: DataSource) {
  await dataSource.destroy();
}
```
````

````

### Шаг 5: Обновить DatabaseConnection

**Файл:** `templates/components/database/base/connection.md`

```typescript
import { createTypeORMConnection, typeormHealthCheck, typeormDisconnect } from "./typeorm/connection";
import type { DataSource } from "typeorm";

// В constructor:
if (orm === ORMType.DRIZZLE) {
  // ... существующий код
} else if (orm === ORMType.TYPEORM) {
  this.connection = createTypeORMConnection(databaseUrl);
} else {
  throw new Error(`Unsupported ORM: ${orm}`);
}

// В disconnect():
} else if (this.orm === ORMType.TYPEORM) {
  const dataSource = this.connection as DataSource;
  await typeormDisconnect(dataSource);
}

// В healthCheck():
} else if (this.orm === ORMType.TYPEORM) {
  await typeormHealthCheck(this.connection as DataSource);
}
````

### Шаг 6: Обновить Database Node UI

**Файл:** React Flow компонент для Database Node

Добавь новый ORM в селектор:

```typescript
const ORM_OPTIONS = [
  { value: "drizzle", label: "Drizzle ORM" },
  { value: "typeorm", label: "TypeORM" }, // ← Добавь
];
```

### Шаг 7: Обновить Генерацию Кода

**Файл:** Code generation logic

```typescript
function generateORMSchema(orm: ORMType, entity: Entity) {
  if (orm === ORMType.DRIZZLE) {
    return generateFromTemplate("components/drizzle/schema.md", entity);
  } else if (orm === ORMType.TYPEORM) {
    return generateFromTemplate("components/typeorm/schema.md", entity);
  }
  // ...
}
```

## Добавление нового типа БД

### Шаг 1: Добавить Database Type

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export enum DatabaseType {
  POSTGRESQL = "postgresql",
  MYSQL = "mysql",
  MONGODB = "mongodb", // ← Добавь
  REDIS = "redis", // ← Добавь
}
```

### Шаг 2: Обновить Database Node

**Файл:** `templates/DATABASE_NODES.md`

Добавь новый тип в UI селектор и в структуру `DatabaseNodeData`.

### Шаг 3: Создать Компоненты для БД

Если нужны специфичные компоненты для БД (например, для MongoDB нужен другой подход):

```
templates/components/
├── mongodb/
│   ├── connection.ts.md
│   └── collection.md  # Вместо schema/repository
```

## Добавление нового компонента

### Шаг 1: Добавить Component Type

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export enum ComponentType {
  // ... существующие
  CUSTOM_COMPONENT = "custom-component", // ← Добавь
}
```

### Шаг 2: Создать Template

**Файл:** `templates/components/custom-component.md`

````markdown
# 📦 Component: Custom Component

## Описание

Описание компонента.

## 📄 Код

```typescript
// Код компонента
```
````

````

### Шаг 3: Зарегистрировать в Blueprint

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export const REQUIRED_COMPONENTS: Record<BlueprintType, ComponentType[]> = {
  [BlueprintType.SERVICE]: [
    // ... существующие
    ComponentType.CUSTOM_COMPONENT, // ← Добавь если нужно
  ],
};
````

## Добавление нового blueprint

### Шаг 1: Добавить Blueprint Type

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export enum BlueprintType {
  // ... существующие
  CUSTOM_BLUEPRINT = "custom-blueprint", // ← Добавь
}
```

### Шаг 2: Создать Blueprint Структуру

**Структура:**

```
templates/blueprints/
└── custom-blueprint/
    ├── README.md          # Описание blueprint
    ├── structure.md       # Какие компоненты использовать
    └── service.ts.md     # LLM template (если нужен)
```

### Шаг 3: Зарегистрировать Компоненты

**Файл:** `templates/TYPES_AND_CONSTANTS.md`

```typescript
export const REQUIRED_COMPONENTS: Record<BlueprintType, ComponentType[]> = {
  [BlueprintType.CUSTOM_BLUEPRINT]: [
    ComponentType.MAIN_ENTRY,
    ComponentType.HEALTH_CHECK,
    // ... другие компоненты
  ],
};
```

## Checklist для расширения

### Для Нового ORM

- [ ] Добавлен `ORMType` в enum
- [ ] Добавлены `ComponentType` для schema и repository
- [ ] Зарегистрирован в `ORM_TEMPLATES`
- [ ] Созданы компоненты в `components/{orm}/`
- [ ] Обновлен `DatabaseConnection`
- [ ] Обновлен UI для Database Node
- [ ] Обновлена логика генерации кода
- [ ] Добавлены примеры в документацию

### Для Нового Компонента

- [ ] Добавлен `ComponentType` в enum
- [ ] Создан template в `components/`
- [ ] Зарегистрирован в `REQUIRED_COMPONENTS` (если нужно)
- [ ] Обновлена документация

### Для Нового Blueprint

- [ ] Добавлен `BlueprintType` в enum
- [ ] Создана структура в `blueprints/{name}/`
- [ ] Зарегистрированы компоненты в `REQUIRED_COMPONENTS`
- [ ] Обновлена документация

## Примеры расширений

### Пример 1: Добавить TypeORM

См. шаги выше. TypeORM — хороший пример, потому что:

- Имеет свой формат schema (декораторы)
- Имеет свой API
- Требует настройки DataSource

### Пример 2: Добавить MongoDB

MongoDB отличается от SQL БД:

- Не нужна schema в традиционном смысле
- Используются collections вместо tables
- Нужны другие компоненты (connection, collection, document)

**Структура:**

```
components/
└── mongodb/
    ├── connection.ts.md    # MongoDB connection
    └── collection.md       # Collection setup (вместо schema)
```

### Пример 3: Добавить GraphQL Blueprint

Новый blueprint для GraphQL API:

```
blueprints/
└── graphql-service/
    ├── README.md
    ├── structure.md
    └── resolver.ts.md  # LLM генерит resolvers
```

## 🔗 Связанные Документы

- **[`TYPES_AND_CONSTANTS.md`](./TYPES_AND_CONSTANTS)** — Все типы и енамы
- **[`DATABASE_NODES.md`](./DATABASE_NODES)** — Database Nodes система
- **[`README.md`](./README)** — Обзор системы templates

---

_Архитектура спроектирована для легкого расширения. Добавляй новые ORM, БД и компоненты следуя этим принципам._
