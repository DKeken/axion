---
title: "Контракты и валидация"
description: "Система контрактов и валидации в Axion: Protobuf контракты, автоматическая генерация, Validation Pipeline (6 уровней)."
order: 4
---

# Контракты и валидация

<Callout type="info" title="Связанные документы">
  Этот документ дополняет [основную архитектуру](./ARCHITECTURE) деталями
  системы контрактов и валидации.
</Callout>

## Обзор

Все микросервисы используют Protobuf контракты для типобезопасной коммуникации, а многоуровневая система валидации гарантирует качество сгенерированного кода.

**Что здесь описано:**

- Protobuf контракты — как создаются и используются
- Автоматическая генерация контрактов из графа
- Validation Pipeline — 6 уровней валидации
- Паттерны отказоустойчивости

---

## Контрактная система

### Что такое Protobuf контракты?

Protobuf (Protocol Buffers) — это язык-независимый формат сериализации данных, который используется для типобезопасной коммуникации между микросервисами.

<Callout type="idea" title="Преимущества Protobuf">
  - **Типобезопасность** — автоматическая генерация TypeScript типов -
  **Производительность** — более компактный формат чем JSON -
  **Версионирование** — поддержка обратной совместимости - **Валидация** —
  встроенная валидация данных
</Callout>

### Protobuf Contracts в Axion

Все межсервисное общение через RabbitMQ использует Protobuf для типобезопасности:

```protobuf
syntax = "proto3";
package axion.product;

service ProductService {
  rpc CreateProduct(CreateProductRequest) returns (ProductResponse);
  rpc GetProduct(GetProductRequest) returns (ProductResponse);
}

message CreateProductRequest {
  string name = 1;
  double price = 2;
  string description = 3;
}

message ProductResponse {
  string id = 1;
  string name = 2;
  double price = 3;
  string description = 4;
}
```

### Автоматическая Генерация

Контракты генерируются автоматически из графа архитектуры:

**Процесс генерации:**

1. **Извлечение из графа** — система анализирует edges между сервисами
   - Определяет какие сервисы взаимодействуют
   - Определяет какие действия нужны (create, get, update, delete)

2. **Генерация Protobuf** — создаются `.proto` файлы
   - Для каждого сервиса создается свой `.proto` файл
   - Учитываются связи между сервисами (edges)

3. **Генерация TypeScript** — генерируются типы из `.proto`
   - Используется `protoc` с плагином `ts-proto`
   - Автоматическая генерация типов и сервисов

4. **Валидация** — проверка соответствия MessagePattern
   - Все MessagePattern должны иметь соответствующие контракты
   - Все контракты должны иметь реализованные MessagePattern

### Генерация TypeScript из Protobuf

```bash
# Генерация TypeScript типов и сервисов
protoc \
  --plugin=protoc-gen-ts_proto=./node_modules/.bin/protoc-gen-ts_proto \
  --ts_proto_out=./src/generated \
  --ts_proto_opt=nestJs=true \
  --proto_path=./proto \
  ./proto/**/*.proto
```

**Результат:**

- TypeScript типы для всех сообщений
- TypeScript сервисы для всех RPC методов
- Типобезопасные клиенты и серверы

Подробнее: [`templates/rules/contracts-schema`](./templates/rules/contracts-schema)

---

## Система валидации

### Зачем нужна валидация?

<Callout type="info" title="Многоуровневая валидация">
  Многоуровневая валидация гарантирует качество кода, работоспособность
  сервисов, типобезопасность и соответствие контрактам.
</Callout>

### Validation Pipeline (6 уровней)

После генерации кода выполняется последовательная валидация на 6 уровнях:

| Уровень                   | Что проверяет                                        | Как работает                             | Результат при успехе   | Результат при ошибке                      |
| ------------------------- | ---------------------------------------------------- | ---------------------------------------- | ---------------------- | ----------------------------------------- |
| **1. Structural**         | Обязательные файлы, структура директорий             | Загрузка Blueprint, проверка файлов      | ✅ Переход к уровню 2  | ❌ Остановка, список отсутствующих файлов |
| **2. Contract**           | Соответствие MessagePattern и Protobuf               | Парсинг AST, сравнение с контрактами     | ✅ Переход к уровню 3  | ❌ Ошибка, список несоответствий          |
| **3. TypeScript**         | Корректность типов, отсутствие ошибок                | `tsc --noEmit`                           | ✅ Переход к уровню 4  | ❌ Список ошибок типов                    |
| **4. Build**              | Компиляция и сборка кода                             | `bun run build`                          | ✅ Переход к уровню 5  | ❌ Список ошибок сборки                   |
| **5. Health Check**       | Запуск сервиса, health endpoint, MessagePattern      | Запуск в Docker, проверка endpoints      | ✅ Переход к уровню 6  | ❌ Ошибка, логи для отладки               |
| **6. Contract Discovery** | Валидация при запуске через ContractDiscoveryService | Проверка всех MessagePattern при запуске | ✅ Валидация завершена | ❌ Ошибка несоответствия                  |

### Детали каждого уровня

#### 1. Structural Validation

| Проверка                 | Описание                                   |
| ------------------------ | ------------------------------------------ |
| **Обязательные файлы**   | Наличие всех файлов из Blueprint структуры |
| **Структура директорий** | Правильная организация файлов              |
| **Immutable файлы**      | Файлы из шаблона не изменены               |

#### 2. Contract Validation

| Проверка                      | Описание                           |
| ----------------------------- | ---------------------------------- |
| **MessagePattern → Protobuf** | Все MessagePattern имеют контракты |
| **Protobuf → MessagePattern** | Все контракты реализованы          |
| **Соответствие типов**        | Типы в контрактах и коде совпадают |

#### 3. TypeScript Validation

| Проверка               | Описание                     |
| ---------------------- | ---------------------------- |
| **Корректность типов** | Нет ошибок типизации         |
| **Импорты**            | Все импорты корректны        |
| **Компиляция**         | Код компилируется без ошибок |

#### 4. Build Validation

| Проверка        | Описание                    |
| --------------- | --------------------------- |
| **Компиляция**  | Код успешно компилируется   |
| **Зависимости** | Все зависимости установлены |
| **Сборка**      | Финальный билд успешен      |

#### 5. Health Check Validation

| Проверка            | Описание                            |
| ------------------- | ----------------------------------- |
| **Запуск в Docker** | Сервис успешно запускается          |
| **Health endpoint** | `/health` отвечает корректно        |
| **MessagePattern**  | Все MessagePattern доступны         |
| **Зависимости**     | Подключение к БД, RabbitMQ работает |

#### 6. Contract Discovery Validation

| Проверка                       | Описание                                        |
| ------------------------------ | ----------------------------------------------- |
| **Регистрация MessagePattern** | Все зарегистрированные соответствуют контрактам |
| **Неиспользуемые контракты**   | Нет неиспользуемых контрактов                   |
| **Реализация контрактов**      | Все контракты реализованы                       |

### Результат валидации

| Статус        | Описание                   | Действия                                      |
| ------------- | -------------------------- | --------------------------------------------- |
| ✅ **Успех**  | Все уровни пройдены        | Сервис готов к деплою, статус `validated`     |
| ❌ **Ошибка** | Ошибка на одном из уровней | Статус `error`, список ошибок для исправления |

Подробнее: [`templates/rules/validation-system.md`](./templates/rules/validation-system)

---

## Отказоустойчивость

### Паттерны отказоустойчивости

Каждый сервис должен реализовывать паттерны отказоустойчивости:

| Паттерн               | Назначение                            | Параметры                                | Применение               |
| --------------------- | ------------------------------------- | ---------------------------------------- | ------------------------ |
| **Retry**             | Повторные попытки при временных сбоях | Exponential backoff, max 3-5 попыток     | Network errors, timeouts |
| **Circuit Breaker**   | Защита от каскадных сбоев             | Threshold: 5 ошибок, timeout: 60s        | Внешние зависимости      |
| **Timeout**           | Ограничение времени ожидания          | Default: 5 секунд                        | Все асинхронные операции |
| **Fallback**          | Альтернативные стратегии при сбоях    | Кеш при недоступности БД                 | Критичные операции       |
| **Graceful Shutdown** | Корректное завершение работы          | Закрытие соединений, завершение запросов | Все сервисы              |
| **Health Checks**     | Мониторинг здоровья сервиса           | HEALTHY → DEGRADED → UNHEALTHY           | Постоянный мониторинг    |

### Детали паттернов

#### Retry

| Параметр                  | Значение                 | Описание                             |
| ------------------------- | ------------------------ | ------------------------------------ |
| **Стратегия**             | Exponential backoff      | Задержка увеличивается: 1s → 2s → 4s |
| **Максимум попыток**      | 3-5                      | Зависит от типа операции             |
| **Обрабатываемые ошибки** | Network errors, timeouts | Не retry для 4xx ошибок              |

#### Circuit Breaker

| Состояние     | Условие перехода   | Действие            |
| ------------- | ------------------ | ------------------- |
| **CLOSED**    | Нормальная работа  | Запросы проходят    |
| **OPEN**      | Threshold=5 ошибок | Запросы блокируются |
| **HALF_OPEN** | Timeout=60s        | Тестовые запросы    |

Подробнее: [templates/rules/resilience-patterns](./templates/rules/resilience-patterns)

---

## 📚 Дополнительные Ресурсы

- **[`ARCHITECTURE.md`](./ARCHITECTURE)** — Полная архитектура, включая Validation Pipeline
- **[`templates/rules/contracts-schema.md`](./templates/rules/contracts-schema)** — Правила создания Protobuf контрактов
- **[`templates/rules/validation-system.md`](./templates/rules/validation-system)** — Детали системы валидации
- **[`templates/rules/resilience-patterns.md`](./templates/rules/resilience-patterns)** — Паттерны отказоустойчивости

---

_Для понимания общей архитектуры см. [`ARCHITECTURE.md`](./ARCHITECTURE)._
