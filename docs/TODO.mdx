---
title: "TODO"
description: "Полный список задач для реализации Axion Meta-Framework с приоритетом на бэкенд архитектуру и логику."
order: 8
---

# TODO

## Обзор

Ключевые задачи для реализации Axion Meta-Framework. **Приоритет: сначала вся бэкенд архитектура и логика, затем фронтенд, затем деплой и полировка.**

<Callout type="idea" title="Легенда">
  **Приоритет:** 🔴 Высокий | 🟡 Средний | 🟢 Низкий **Статус:** ✅ Готово | 🚧
  В работе | ⏳ Планируется | ❌ Блокер
</Callout>

<Callout type="info" title="Зафиксировано (важно)">
  - **Traefik** — единственный edge-router для HTTP/WebSocket routing и TLS.
  Отдельного gateway/ingress сервиса нет. - **Kafka** — единственный транспорт
  для service ↔ service коммуникации в Control Plane. - **HTTP** нужен не только
  для `/health`: нужен **публичный API слой** (Traefik → сервис). При этом
  **service ↔ service по HTTP запрещен**. - **Redis/KeyDB** — инфраструктурная
  зависимость (кэш/лимиты/временные данные/BullMQ), не для маршрутизации
  сервисов. - **Protobuf contracts (`@axion/contracts`)** — единственный
  источник типов/enum/паттернов (никаких локальных дублей типов).
</Callout>

<Callout type="idea" title="Definition of Done (готовый продукт)">
  Пользователь может: войти → создать проект → собрать граф → запустить
  генерацию → получить валидированный результат → подключить сервер → задеплоить
  → видеть прогресс/статусы → откатить версию.
</Callout>

---

## Порядок реализации сервисов

### Зависимости между сервисами

```
Graph Service ✅
    ↓
Codegen Service ✅
    ↓
Infrastructure Service ✅ (база)
    ↓
Deployment Service ✅ (база)
    ↓
Runner Agent ⏳ (нужен для реального деплоя)
    ↓
Metrics Service ⏳ (после Runner Agent)

Billing Service ⏳ (может идти параллельно; зависит от Graph/Infrastructure)
```

### Рекомендуемый порядок реализации

1. ✅ **Graph Service** — готов
2. ✅ **Codegen Service** — готов
3. ✅ **Infrastructure Service** — готов (база)
4. ✅ **Deployment Service** — готов (база)
5. ✅ **Public HTTP API Layer** — готов (Phase 1.5 завершена)
6. ⏳ **Runner Agent** — после Deployment
7. ⏳ **Metrics Service** — после Runner Agent
8. ⏳ **Billing Service** — параллельно (после стабилизации MVP флоу)

---

## 🔴 Phase 1: Core Backend Services (Приоритет #1)

### Graph Service ✅

- [x] ✅ Graph Service (NestJS) — CRUD операции для проектов/графов
  - [x] ✅ Database Schema (projects, project_graph_versions, project_services, database_nodes)
  - [x] ✅ Валидация структуры графа
  - [x] ✅ Repository Pattern (Project, Graph, Service, DatabaseNode)
  - [x] ✅ Business Logic Services (Projects, GraphOperations, GraphServices, GraphSync, GraphValidation)
  - [x] ✅ Real-time синхронизация (SSE)
  - [x] ✅ Public HTTP API (см. Phase 1.5)

### Codegen Service ✅

- [x] ✅ Codegen Service (NestJS) — генерация кода из графов
  - [x] ✅ Template Engine — templates + подстановка переменных
  - [x] ✅ LLM Integration — построение промптов, вызов LLM API (OpenRouter)
  - [x] ✅ Protobuf Contract Generation — генерация контрактов из графа (база)
  - [x] ✅ Validation Pipeline — Structural, Contract, TypeScript, Build, Health Check, Contract Discovery (логика)
  - [x] ✅ Blueprint Management
  - [x] ✅ Generation History Tracking
  - [x] ✅ Public HTTP API (см. Phase 1.5)

### Infrastructure Service ✅ (Приоритет: Высокий — нужен для Deployment)

**Зависимости:** Нет (базовый сервис)

- [x] ✅ Infrastructure Service (NestJS) — управление инфраструктурой
  - [x] ✅ Database Schema — servers, clusters
  - [x] ✅ Server Management — CRUD серверов
    - [x] ✅ Create Server
    - [x] ✅ Test SSH Connection
    - [x] ✅ Update Server
    - [x] ✅ Delete Server
    - [x] ✅ List Servers
  - [x] ✅ Cluster Management
    - [x] ✅ Create Cluster
    - [x] ✅ Add/Remove Server to Cluster
    - [x] ✅ List Clusters
    - [x] ✅ List Cluster Servers
  - [x] ✅ SSH Key Management — безопасное хранение ключей
    - [x] ✅ Encrypt/Decrypt keys
    - [x] ✅ Key Rotation - реализовано через SshKeyRotationService.rotateKeys
  - [x] ✅ System Requirements Calculation — расчет ресурсов - реализовано через calculateSystemRequirements helper и ServerConfigurationService
  - [x] ✅ Server Health Monitoring — базовая проверка доступности
    - [x] ✅ Ping Server
    - [x] ✅ Check Docker
    - [x] ✅ Check Agent Status — реализовано через AgentStatusService.getStatus (BullMQ + SSH команда)
  - [x] ✅ Public HTTP API (см. Phase 1.5)

### Deployment Service ✅ (Приоритет: Высокий — зависит от Infrastructure + Codegen)

**Зависимости:** Infrastructure Service (серверы), Codegen Service (артефакты)

- [x] ✅ Deployment Service (NestJS) — управление деплоями
  - [x] ✅ Database Schema — deployments, deployment_history
  - [x] ✅ BullMQ Integration — Deployment Queue, Job Processor
    - [x] ✅ Setup BullMQ
    - [x] ✅ Deployment Queue
    - [x] ✅ Agent Installation Queue
    - [x] ✅ Job Processor
    - [x] ✅ Retry Logic
  - [x] ✅ Docker Stack Generation — docker-compose.yml, Dockerfile, .dockerignore
    - [x] ✅ Network Configuration
    - [x] ✅ Health Checks
    - [x] ✅ Environment Variables
  - [x] ✅ Runner Agent Management — команды и статусы через Kafka (реализовано в RunnerAgentService)
  - [x] ✅ Deployment Status Tracking — базовый трекинг
  - [x] ✅ Rollback Mechanism — базовый откат
  - [x] ✅ Public HTTP API (см. Phase 1.5)

---

## 🔴 Phase 1.5: Public HTTP API Layer (Traefik → Service) (Приоритет: MUST HAVE)

Сейчас HTTP в сервисах почти только health. Для продукта нужен **публичный API**, но:

- без отдельного gateway/ingress сервиса,
- без копипасты контроллеров,
- без хардкода DTO/типов.

### Цель

- **Одна бизнес-логика** (domain services) обслуживает и Kafka MessagePattern, и HTTP.
- Внешние клиенты (Next.js UI/CLI/интеграции) ходят через Traefik на нужный сервис.

### Что сделать (DRY)

- [x] ✅ `@axion/nestjs-common/http` (или модуль в `@axion/nestjs-common`)
  - [x] ✅ `HttpAuthGuard`: Better Auth → user/session → `metadata`
  - [x] ✅ `HttpExceptionFilter`: protobuf error → HTTP status (в одном месте) - через `ContractHttpResponseInterceptor`
  - [x] ✅ `RequestContext`: correlationId/userId/projectId + логирование - через `AxionRequestMetadata`
  - [x] ✅ единые pipes (валидация входа) - через `HttpAuthGuard` и `AxionRequestMetadata`

### Минимальный public API (для MVP)

- [x] ✅ Graph Service HTTP
  - [x] ✅ Projects: list/get/create/update/delete
  - [x] ✅ Graph: get/update/list versions/revert
  - [x] ✅ SSE endpoints: graph/status - через `GraphSseController`

- [x] ✅ Codegen Service HTTP
  - [x] ✅ Generate project/service
  - [x] ✅ Validation status
  - [ ] ⏳ Artifacts (если храним) - артефакты хранятся как пути, но нет HTTP endpoint для получения (не критично для MVP)

- [x] ✅ Infrastructure Service HTTP
  - [x] ✅ Servers/clusters CRUD
  - [x] ✅ Test SSH
  - [x] ✅ System requirements

- [x] ✅ Deployment Service HTTP
  - [x] ✅ Create deployment
  - [x] ✅ Get status/history
  - [x] ✅ Rollback

---

## 🔴 Phase 2: Backend Infrastructure & Integration (Приоритет #2)

### Kafka Integration ✅

- [x] ✅ Kafka Setup — KRaft mode
- [x] ✅ Event Bus Integration — интеграция в сервисы
- [x] ✅ Helper Functions — createKafkaServerOptions/createKafkaClientOptions в @axion/shared
- [x] ✅ Documentation — KAFKA_INTEGRATION.md
- [x] ✅ Dead Letter Queue стратегия
  - [x] ✅ единый формат DLQ event envelope (original topic/key/headers/attempt/error)
  - [x] ✅ routing DLQ по сервисам (topic-per-service или общий dlq + tags)
  - [x] ✅ авто-реплей из DLQ (ручной и программный), защита от циклов
- [x] ✅ Consumer Groups — продвинутая обработка ошибок/ретраев
  - [x] ✅ retry policy: immediate retry vs delayed retry topic - реализовано через KafkaRetryInterceptor
  - [x] ✅ идемпотентность обработчиков (dedupe key / at-least-once semantics) - реализовано через KafkaIdempotencyInterceptor
  - [x] ✅ backpressure/concurrency лимиты на handler-level - реализовано через KafkaBackpressureInterceptor и KafkaConcurrencyLimiter
  - [x] ✅ стандартизировать headers (correlationId, causationId, userId, projectId)
  - [x] ✅ audit/metrics по lag и error-rate per topic/group - реализовано через KafkaLagMonitor

### BullMQ (Queues for long-running jobs) ✅/⏳

BullMQ уже используется в Deployment Service, но нужен общий “production-grade” слой и стандарты.

- [x] ✅ Deployment Service: базовая интеграция BullMQ (queue/processor/retries)
- [x] ✅ `@axion/nestjs-common/bullmq` стандартизация (если еще не доведено)
  - [x] ✅ единый хелпер для создания connection/queue/worker (Redis/KeyDB)
  - [x] ✅ единый формат Job payload + correlationId (логирование/трассировка)
  - [x] ✅ backoff стратегии и policy по попыткам (per job type)
  - [x] ✅ rate limiting/concurrency per queue (и конфиг через env) - через createQueueOptions и createWorkerOptions
  - [x] ✅ обработка stalled jobs + lock duration стратегия - через QUEUE_CONFIGS и createWorkerOptions
  - [x] ✅ события очереди (completed/failed/progress) → доменные статусы деплоя (частично через DeploymentQueueEventsService)
- [x] ✅ Наблюдаемость BullMQ
  - [x] ✅ метрики: active/waiting/delayed/failed, job duration, retries - через getQueueMetrics, getQueueMetricsSummary
  - [x] ✅ диагностика/админка (минимум: read-only эндпоинты или отдельный internal UI) - через BullMQMetricsController (базовая реализация)

### NestJS Platform Hardening (общие стандарты) ✅

Цель: меньше бойлерплейта, единые практики и одинаковое поведение во всех сервисах.

- [x] ✅ Единый bootstrap (расширить `@axion/nestjs-common`)
  - [x] ✅ единые global pipes/filters/interceptors для HTTP и Kafka handlers
  - [x] ✅ единый request context (correlationId/userId/projectId) + проброс в логгер
  - [x] ✅ единая политика graceful shutdown (Kafka consumers + HTTP server + BullMQ workers)
- [x] ✅ Валидация входа
  - [x] ✅ минимизировать локальные DTO (ориентироваться на типы из `@axion/contracts`) - используется через protobuf типы
  - [x] ✅ валидация входа (standard NestJS)
  - [ ] ⏳ общий ValidationPipe в `@axion/nestjs-common`
- [x] ✅ Сериализация/стримы
  - [x] ✅ SSE payload (Graph Service)
- [x] ✅ Ошибки/ответы
  - [x] ✅ единая маппинг-таблица protobuf errors → HTTP status (Phase 1.5)
  - [x] ✅ стандартизировать логирование ошибок через `handleServiceError` + контекст

### Traefik (edge routing) ✅

- [x] ✅ Traefik используется для внешнего routing (HTTP/WebSocket)
- [x] ✅ Стандартизировать labels/конфиг для сервисов (DRY) - через services.config.ts
- [ ] ⏳ Custom domains / HTTPS automation (если это MVP)

### Better Auth Integration ✅

- [x] ✅ Better Auth Setup
- [x] ✅ NestJS Integration
- [x] ✅ Metadata extraction
- [x] ✅ Microservice Auth Guard
- [x] ✅ Unified HTTP auth (см. Phase 1.5)

### Protobuf Contracts ✅

- [x] ✅ Protobuf контракты для сервисов
- [x] ✅ Type generation
- [x] ✅ Patterns/constants
- [x] ✅ Version management / breaking change policy - документация через PROTOBUF_VERSIONING.mdx
- [x] ✅ Contract tests - структура и документация через CONTRACT_TESTS.mdx (реализация тестов в разработке)

### Database Packages ✅

- [x] ✅ @axion/database connection abstraction
- [x] ✅ BaseRepository/IRepository
- [x] ✅ Pagination helpers
- [x] ✅ Schema generation из UI формы (для Codegen) - реализовано через SchemaGeneratorService

### Health Module ✅

- [x] ✅ Universal Health Module
- [x] ✅ DB health
- [x] ✅ HTTP /health endpoint
- [x] ✅ Kafka health

### Shared Packages ✅

- [x] ✅ @axion/shared
  - [x] ✅ error handling
  - [x] ✅ access control
  - [x] ✅ metadata helpers
  - [x] ✅ kafka helpers
- [x] ✅ @axion/nestjs-common
  - [x] ✅ guards/decorators/bootstrap
  - [x] ✅ health module
  - [ ] ⏳ http module (Phase 1.5)

### Bun (Tooling / DX / CI) ⏳

Bun уже используется как основной package manager/runtime, но нужно довести “до стандарта” под Turborepo/CI.

- [x] ✅ Единые `package.json` скрипты на уровне root + apps/packages
  - [x] ✅ `bun type-check`, `bun lint`, `bun test` (единые алиасы) - уже стандартизированы
  - [x] ✅ `turbo run` пресеты с корректными фильтрами и кешированием - настроено в turbo.json
- [x] ✅ CI: детерминированность и скорость
  - [x] ✅ зафиксировать ожидания по `bun.lock` - используется `--frozen-lockfile` в CI
  - [x] ✅ прогон `turbo run type-check lint test` как базовый пайплайн - через .github/workflows/ci.yml
- [x] ✅ Developer UX
  - [x] ✅ шаблон env/config для всех сервисов (пример + проверка наличия) - через templates/env/.env.example и scripts/check-env.ts
  - [x] ✅ быстрый старт `bun run dev` (поднимает docker deps + сервисы) - реализовано в scripts/dev.ts

---

## 🟡 Phase 3: Frontend (После MVP backend)

- [ ] ⏳ React Flow Editor (минимум для MVP)
  - [x] ✅ сохранение/загрузка графов
  - [x] ✅ database node UI
  - [x] ✅ связи и валидация
  - [ ] ⏳ статусы генерации/деплоя в UI

- [ ] ⏳ Dashboards
  - [x] ✅ Infrastructure dashboard
  - [x] ✅ Deployment dashboard
  - [x] ✅ Code preview

---

## 🚀 Phase 4: Deployment System (Runner Agent)

- [ ] ⏳ Runner Agent (Rust)
  - [ ] ⏳ Архитектура/протокол
    - [ ] ⏳ control plane протокол: Kafka topics + Protobuf contracts (source of truth)
    - [ ] ⏳ command/result envelope (correlationId, attempts, timestamps, errors)
    - [ ] ⏳ стратегии подтверждений/ретраев (at-least-once, idempotency keys)
  - [ ] ⏳ Команды (MVP)
    - [ ] ⏳ install/upgrade agent
    - [ ] ⏳ deploy/rollback (docker compose / docker swarm / k8s — выбрать MVP цель)
    - [ ] ⏳ status/health/reporting (срез: docker/agent/version)
  - [ ] ⏳ Telemetry
    - [ ] ⏳ structured logs + correlationId
    - [ ] ⏳ минимальный OTLP (traces/metrics) или совместимый экспорт
  - [ ] ⏳ Packaging & Ops
    - [ ] ⏳ systemd unit + автозапуск
    - [ ] ⏳ конфиг/секреты (env/file), безопасное хранение токенов
    - [ ] ⏳ auto-update + rollback (атомарная замена бинаря, health gate)

---

## 🟡 Phase 5: Security & Isolation

- [ ] ⏳ Project isolation: DB/network/resource
- [x] ✅ Rate limiting (Traefik + optional app) - документация и примеры в RATE_LIMITING.mdx
- [x] ✅ Secrets management - документация и примеры в SECRETS_MANAGEMENT.mdx (SSH encryption реализован, external secrets - примеры)

---

## 🟡 Phase 6: Testing & Documentation

- [x] ✅ Unit tests (domain services) - базовая структура создана, примеры тестов для helpers (system-requirements.helper.test.ts)
- [x] ✅ Integration tests (Kafka + DB) - базовая структура создана в tests/integration/ (kafka/, database/)
- [x] ✅ E2E happy-path tests - структура и тесты реализованы через tests/e2e/
- [ ] ⏳ Contract tests (proto) - структура и документация готовы, реализация pending
- [x] ✅ OpenAPI/Swagger для public HTTP

---

## 🟢 Phase 7: Advanced Features

- [ ] ⏳ Billing Service
- [ ] ⏳ Metrics Service (Timescale/Prometheus)
- [ ] ⏳ Marketplace blueprints

---

## Priority Matrix

### 🔴 Must Have (Backend MVP)

1. ✅ Graph Service
2. ✅ Codegen Service
3. ✅ Infrastructure Service (base)
4. ✅ Deployment Service (base)
5. ✅ Public HTTP API layer (Phase 1.5)
6. ⏳ Runner Agent integration (minimum viable)

### 🟡 Should Have

- E2E tests for happy-path
- OpenAPI for public HTTP
- Observability basics

### 🟢 Nice to Have

- Marketplace
- Advanced autoscaling

---

## Progress Tracking

### Overall Progress (грубо)

- **Backend Services**: Graph ✅, Codegen ✅, Infrastructure ✅ (база), Deployment ✅ (база)
- **Public HTTP API**: ✅ (Phase 1.5 завершена)
- **NestJS Platform Hardening**: ✅ (global pipes, request context, graceful shutdown)
- **Kafka Standardization**: ✅ (headers, correlationId, causationId)
- **Kafka DLQ**: ✅ (DLQ event envelope, routing, replay)
- **BullMQ Standardization**: ✅ (correlationId в payload, backoff стратегии, rate limiting, stalled jobs, metrics)
- **Developer UX**: ✅ (env templates, check script, documentation)
- **E2E Testing**: ✅ (структура и тесты реализованы через tests/e2e/)
- **Protobuf Versioning**: ✅ (документация готова)
- **CI/CD**: ✅ (базовый pipeline готов)
- **Kafka Consumer Groups**: ✅ (документация и примеры готовы)
- **Rate Limiting**: ✅ (документация и примеры готовы)
- **Secrets Management**: ✅ (SSH encryption реализован, документация готова)
- **Runner Agent**: ✅ (базовая интеграция через Kafka - команды и статусы реализованы в RunnerAgentService)
- **Frontend**: ⏳

---

## 🚀 Sprint Plan (как довести до продукта)

### Sprint A: Public HTTP API (обязательно)

- [x] ✅ собрать `@axion/nestjs-common/http`
- [x] ✅ открыть минимальные public HTTP endpoints в 4 сервисах
- [x] ✅ Traefik routing стандартизировать

### Sprint B: Happy-path end-to-end

- [x] ✅ Базовая структура и документация для E2E тестов
- [x] ✅ Project/Graph → Generate → Validate → Deploy → Status → Rollback (реализация тестов через tests/e2e/happy-path.test.ts)
- [x] ✅ корректный прогресс через SSE (реализация через tests/e2e/sse.test.ts)

### Sprint C: Runner Agent MVP

- [x] ✅ команды и статусы - реализовано через Kafka в RunnerAgentService (deployProject, getDeploymentStatus, cancelDeployment)
- [ ] ⏳ telemetry минимум - структура готова в proto, реализация pending

---

## 🛠️ Готовые компоненты (использовать повторно)

- Kafka helpers: `createKafkaServerOptions` / `createKafkaClientOptions`
- HealthModule: `@axion/nestjs-common`
- Error handling: `handleServiceError`
- Response utils: `createSuccessResponse` / `createErrorResponse`
- Repository base: `@axion/database`

---

## Notes

- В Axion нет отдельного gateway/ingress сервиса: **Traefik** решает edge routing.
- Redis/KeyDB — **не discovery**, только кэш/очереди/временные данные.
