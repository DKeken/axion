---
title: "Runner Agent"
description: "Архитектура Axion Runner Agent: Rust агент для деплоя, gRPC + Kafka, Docker Swarm, автообновление."
order: 5
---

# Runner Agent

<Callout type="warn" title="Критически важный компонент">
  Axion Runner Agent — это критически важный компонент для деплоя и управления
  проектами на серверах клиентов.
</Callout>

<Callout type="info" title="Связанные документы">
  - [Архитектура](./ARCHITECTURE) — Phase 5: Deployment описывает интеграцию
  Runner Agent - [Технические детали](./TECHNICAL_DETAILS) — Технические детали
  деплоя через Runner Agent
</Callout>

## Обзор

Агент написан на **Rust** для максимальной производительности и надежности, использует **gRPC + Kafka** для типобезопасного общения с Control Plane и управляет контейнерами через **Docker Swarm** для продвинутой оркестрации.

**Ключевые особенности:**

| Особенность                    | Описание                                                    | Результат                           |
| ------------------------------ | ----------------------------------------------------------- | ----------------------------------- |
| **Высокая производительность** | Rust для минимального потребления ресурсов                  | Низкое использование CPU и памяти   |
| **Один статический бинарник**  | Работает на любом Linux без зависимостей                    | Универсальность, простота установки |
| **Автоматическая установка**   | Устанавливается при добавлении сервера через SSH + BullMQ   | Нулевая настройка                   |
| **Безопасность**               | Проверка целостности, TLS, изоляция, минимальные привилегии | Защита от атак                      |
| **Автообновление**             | Безопасное обновление с rollback                            | Актуальность без риска              |
| **Observability**              | Structured logging, метрики, health checks                  | Полная видимость работы             |
| **Надежность**                 | Circuit breaker, retry, graceful shutdown                   | Отказоустойчивость                  |
| **Real-time**                  | Мгновенная реакция на команды через gRPC + Kafka            | Низкая задержка                     |

**Проблема, которую решает:**

| Проблема                        | Решение                                      |
| ------------------------------- | -------------------------------------------- |
| Прямой SSH деплой неэффективен  | Rust агент с постоянным подключением         |
| Нет локального сбора телеметрии | Локальный сбор метрик на сервере             |
| Нет автономной работы           | Agent работает при проблемах с Control Plane |
| Docker Compose ограничен        | Docker Swarm для продвинутой оркестрации     |
| Нет надежной доставки команд    | Kafka с гарантией доставки                   |

**Решение:**

| Компонент          | Технология                                               | Преимущества                                         |
| ------------------ | -------------------------------------------------------- | ---------------------------------------------------- |
| **Агент**          | Rust                                                     | Максимальная производительность, безопасность памяти |
| **Коммуникация**   | gRPC + Protobuf                                          | Типобезопасность, уже используется в системе         |
| **Event Bus**      | Kafka                                                    | Надежная доставка, гарантия доставки                 |
| **Оркестрация**    | Docker Swarm                                             | Rolling updates, health checks, scaling              |
| **Телеметрия**     | Локальный сбор                                           | Метрики собираются на сервере                        |
| **Автономность**   | Автономная работа                                        | Работа при проблемах с Control Plane                 |
| **Best practices** | Health checks, graceful shutdown, retry, circuit breaker | Отказоустойчивость                                   |

**Интеграция в процесс деплоя:**

Runner Agent интегрирован в процесс деплоя (см. [`ARCHITECTURE.md`](./ARCHITECTURE#phase-5-deployment-с-axion-runner-agent)). Control Plane отправляет команды через **gRPC** и **Kafka** к Runner Agent, который управляет Docker Swarm локально на сервере.

**Установка:**

| Характеристика               | Описание                                   | Преимущества                          |
| ---------------------------- | ------------------------------------------ | ------------------------------------- |
| **Статический бинарник**     | Один файл, работает на любом Linux         | Нет зависимостей, простота            |
| **Автоматическая установка** | Устанавливается при добавлении сервера     | Нулевая настройка                     |
| **Автоконфигурация**         | Все конфигурируется автоматически          | Не требует ручной настройки           |
| **Универсальность**          | Один бинарник для всех Linux дистрибутивов | Ubuntu, Debian, CentOS, RHEL и другие |

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│              Axion Control Plane (SaaS)                      │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Deployment Service (NestJS)                          │  │
│  │  - Создает задачи деплоя                              │  │
│  │  - Отправляет команды через gRPC                      │  │
│  │  - Публикует события в Kafka                          │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Metrics Ingestion Service                             │  │
│  │  - Принимает метрики через gRPC                       │  │
│  │  - Сохраняет в TimescaleDB                            │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Runner Management Service                             │  │
│  │  - Регистрация агентов через gRPC                      │  │
│  │  - Мониторинг здоровья агентов                        │  │
│  │  - Управление версиями агентов                       │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Kafka Event Bus                                       │  │
│  │  - Команды деплоя (runner.commands.{agent_id})       │  │
│  │  - События от агентов (runner.events.{agent_id})      │  │
│  │  - Гарантия доставки, retry, ordering                 │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓ gRPC + Kafka
┌─────────────────────────────────────────────────────────────┐
│              Client Server (User's Infrastructure)           │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Axion Runner Agent (Rust Binary)                    │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  gRPC Client                                    │  │  │
│  │  │  - Подключение к Control Plane gRPC             │  │  │
│  │  │  - Регистрация агента                           │  │  │
│  │  │  - Heartbeat каждые 30 секунд                   │  │  │
│  │  │  - Отправка метрик                              │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Kafka Consumer                                 │  │  │
│  │  │  - Подписка на команды (runner.commands.{id})  │  │  │
│  │  │  - Обработка команд деплоя                      │  │  │
│  │  │  - Гарантия доставки, retry                     │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Kafka Producer                                 │  │  │
│  │  │  - Публикация событий (runner.events.{id})      │  │  │
│  │  │  - События деплоя, ошибки, метрики              │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Docker Swarm Manager                            │  │  │
│  │  │  - Управление Swarm stack                        │  │  │
│  │  │  - Rolling updates                               │  │  │
│  │  │  - Health checks                                 │  │  │
│  │  │  - Scaling сервисов                              │  │  │
│  │  │  - Управление сетями и volumes                   │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Telemetry Collector                            │  │  │
│  │  │  - Сбор метрик Docker Swarm                      │  │  │
│  │  │  - Сбор системных метрик (CPU, Memory, Disk)    │  │  │
│  │  │  - Агрегация данных                             │  │  │
│  │  │  - Отправка через gRPC                          │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Log Collector                                  │  │  │
│  │  │  - Сбор логов контейнеров                       │  │  │
│  │  │  - Ротация логов                                │  │  │
│  │  │  - Отправка через Kafka (опционально)           │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Command Executor                               │  │  │
│  │  │  - Выполнение команд от Control Plane            │  │  │
│  │  │  - Валидация команд                             │  │  │
│  │  │  - Безопасное выполнение                        │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Docker Swarm Stack                                   │  │
│  │  /opt/axion/projects/{project_id}/                    │  │
│  │    ├── docker-stack.yml                              │  │
│  │    ├── .env                                           │  │
│  │    └── logs/                                          │  │
│  │                                                       │  │
│  │  Swarm Services:                                      │  │
│  │  - {service-1} (replicas: 2)                         │  │
│  │  - {service-2} (replicas: 1)                         │  │
│  │  - Health checks, rolling updates, scaling            │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## Конфигурация агента

### Переменные Окружения

```bash
# Обязательные
AGENT_TOKEN=xxx                    # Токен для аутентификации
CONTROL_PLANE_GRPC_URL=https://... # URL Control Plane gRPC сервера
KAFKA_BROKERS=kafka1:9092,kafka2:9092  # Список Kafka brokers

# Опциональные
AGENT_VERSION=1.0.0                # Текущая версия агента
UPDATE_CHANNEL=stable               # Канал обновлений (stable/beta/alpha)
AUTO_UPDATE_ENABLED=true            # Автоматическое обновление
LOG_LEVEL=info                      # Уровень логирования (trace/debug/info/warn/error)
METRICS_INTERVAL=10                 # Интервал сбора метрик (секунды)
HEARTBEAT_INTERVAL=30               # Интервал heartbeat (секунды)
UPDATE_CHECK_INTERVAL=3600          # Интервал проверки обновлений (секунды)
MAX_RETRIES=3                       # Максимальное количество retry
CIRCUIT_BREAKER_THRESHOLD=5         # Порог для circuit breaker
RATE_LIMIT_RPS=100                  # Rate limit (запросов в секунду)
```

### Файл Конфигурации

```toml
# /etc/axion/agent.toml
[agent]
token = "xxx"  # Или из переменной окружения
version = "1.0.0"
update_channel = "stable"
auto_update_enabled = true

[control_plane]
grpc_url = "https://control.axion.dev:443"
timeout = 30
retry_attempts = 3

[kafka]
brokers = ["kafka1:9092", "kafka2:9092"]
consumer_group = "runner-agent-{agent_id}"
enable_ssl = true
ssl_ca_location = "/etc/axion/certs/kafka-ca.pem"

[telemetry]
enabled = true
interval_seconds = 10
batch_size = 100
send_timeout = 5

[health]
check_interval = 30
timeout = 10
failure_threshold = 3

[update]
check_interval = 3600
download_timeout = 300
install_timeout = 60
rollback_on_failure = true
max_backups = 5

[logging]
level = "info"
format = "json"  # json или text
file = "/var/log/axion/agent.log"
max_size_mb = 100
max_files = 5

[security]
tls_verify = true
tls_ca_location = "/etc/axion/certs/ca.pem"
min_tls_version = "1.2"
```

---

## Требования и совместимость

### Поддерживаемые платформы

| Дистрибутив      | Версия               | Архитектуры     | Статус            |
| ---------------- | -------------------- | --------------- | ----------------- |
| **Ubuntu**       | 18.04+               | x86_64, aarch64 | ✅ Поддерживается |
| **Debian**       | 9+                   | x86_64, aarch64 | ✅ Поддерживается |
| **CentOS**       | 7+                   | x86_64, aarch64 | ✅ Поддерживается |
| **RHEL**         | 7+                   | x86_64, aarch64 | ✅ Поддерживается |
| **Amazon Linux** | 2                    | x86_64, aarch64 | ✅ Поддерживается |
| **Другие Linux** | glibc 2.17+ или musl | x86_64, aarch64 | ✅ Поддерживается |

| Архитектура         | Статус            | Примечания           |
| ------------------- | ----------------- | -------------------- |
| **x86_64 (amd64)**  | ✅ Поддерживается | Основная архитектура |
| **aarch64 (arm64)** | ✅ Поддерживается | ARM серверы          |
| **armv7**           | ⏳ Планируется    | В разработке         |
| **armv6**           | ⏳ Планируется    | В разработке         |

### Минимальные требования

| Ресурс      | Минимум                | Рекомендуется         | Описание                                       |
| ----------- | ---------------------- | --------------------- | ---------------------------------------------- |
| **CPU**     | 1 core                 | 2+ cores              | Для обработки команд и управления контейнерами |
| **Memory**  | 256MB                  | 512MB+                | Для работы агента и метрик                     |
| **Disk**    | 50MB                   | Зависит от проектов   | Бинарник + место для проектов                  |
| **Docker**  | 20.10+                 | Последняя версия      | С поддержкой Swarm                             |
| **Network** | Доступ к Control Plane | Стабильное соединение | gRPC и Kafka brokers                           |

### Статическая сборка

Агент собирается как **один статический бинарник** без зависимостей:

| Характеристика           | Описание                              | Преимущества             |
| ------------------------ | ------------------------------------- | ------------------------ |
| **Статическая линковка** | Все библиотеки включены в бинарник    | Нет внешних зависимостей |
| **musl libc**            | Для максимальной совместимости        | Работает на любом Linux  |
| **Нет зависимостей**     | Не требует установки библиотек        | Простота развертывания   |
| **Один файл**            | Просто скопировать и запустить        | Удобство                 |
| **Размер**               | ~5-10MB (сжатый), ~15-20MB (несжатый) | Компактность             |

**Проверка статической линковки:**

```bash
# После сборки
ldd target/x86_64-unknown-linux-musl/release/axion-agent
# Должно вывести: "not a dynamic executable"

file target/x86_64-unknown-linux-musl/release/axion-agent
# Должно вывести: "statically linked"
```

---

## Компоненты Runner Agent

Агент состоит из следующих ключевых компонентов:

1. **gRPC Client** — подключение к Control Plane, регистрация, heartbeat, отправка метрик
2. **Kafka Consumer** — подписка на команды от Control Plane, гарантия доставки
3. **Kafka Producer** — публикация событий деплоя, ошибок, метрик
4. **Docker Swarm Manager** — управление Swarm stack, rolling updates, health checks, scaling
5. **Telemetry Collector** — сбор метрик Docker Swarm и системных метрик, отправка через gRPC
6. **Log Collector** — сбор и ротация логов контейнеров
7. **Command Executor** — безопасное выполнение команд от Control Plane
8. **Configuration Manager** — загрузка и hot reload конфигурации
9. **Metrics Exporter** — экспорт метрик в Prometheus формат
10. **Backup Manager** — резервное копирование конфигураций проектов

Все компоненты реализованы на Rust для максимальной производительности и надежности.

---

## 🔐 Безопасность

### 1. Аутентификация Агента

- **Agent Token:** Уникальный токен для каждого агента, генерируется при регистрации
- **Хранение:** Токен хранится в `/etc/axion/agent.token` (зашифрован)
- **gRPC TLS:** Все соединения через TLS
- **Ротация:** Токен можно ротировать через Control Plane

### 2. Валидация Команд

- **Whitelist:** Только разрешенные команды могут выполняться
- **Sanitization:** Проверка параметров команд
- **Timeout:** Все команды имеют timeout для защиты от зависания
- **Resource Limits:** Ограничения на использование ресурсов

### 3. Изоляция

- **Docker Socket:** Агент работает с Docker socket, но не имеет root прав
- **Network Isolation:** Агент работает в отдельной Docker сети
- **File System:** Доступ только к `/opt/axion/` директории
- **Capabilities:** Минимальные Linux capabilities

### 4. Шифрование

- **TLS:** Все gRPC соединения через TLS (минимум TLS 1.2)
- **Kafka SASL/SSL:** Kafka соединения через SASL/SSL
- **Encrypted Storage:** Токены и секреты хранятся в зашифрованном виде
- **Certificate Pinning:** Опциональная проверка сертификатов Control Plane

### 5. Автообновление

- **Проверка целостности:** SHA256 checksum для всех обновлений
- **Graceful Shutdown:** Корректное завершение работы перед обновлением
- **Rollback:** Автоматический откат при проблемах
- **Каналы обновлений:** Stable, Beta, Alpha для контроля версий
- **Обязательные обновления:** Поддержка критических обновлений безопасности
- **Signed Updates:** Цифровая подпись обновлений (опционально)

### 6. Дополнительные Меры Безопасности

- **Seccomp:** Ограничение системных вызовов через seccomp
- **AppArmor/SELinux:** Профили безопасности для изоляции
- **Capabilities:** Минимальные Linux capabilities (CAP_DAC_OVERRIDE, CAP_NET_BIND_SERVICE)
- **Read-only Root:** Части файловой системы в read-only режиме
- **Network Policies:** Ограничение сетевого доступа
- **Resource Limits:** Ограничения CPU, Memory, Disk I/O
- **Audit Logging:** Логирование всех критических операций

---

## Мониторинг и Alerting

### Метрики для Мониторинга

**Критические метрики:**

- `agent_uptime_seconds` — время работы агента
- `agent_heartbeat_lag_seconds` — задержка последнего heartbeat
- `agent_commands_failed_total` — количество неудачных команд
- `agent_grpc_connection_status` — статус gRPC соединения (0/1)
- `agent_kafka_consumer_lag` — задержка Kafka consumer
- `agent_update_status` — статус последнего обновления (0=success, 1=failed, 2=rolled_back)

**Предупреждающие метрики:**

- `agent_cpu_usage_percent` — использование CPU
- `agent_memory_usage_bytes` — использование памяти
- `agent_docker_operations_duration_seconds` — время операций Docker
- `agent_telemetry_collection_duration_seconds` — время сбора телеметрии

### Alerting Rules (Prometheus)

Ключевые алерты: AgentDown, AgentHeartbeatLag, AgentHighErrorRate, AgentUpdateFailed, AgentHighCPUUsage, AgentHighMemoryUsage.

Настроены через Prometheus Alertmanager для мониторинга здоровья агентов.

### Логирование

**Structured Logging (JSON):** Все логи в формате JSON для удобного парсинга и анализа.

**Уровни:** trace, debug, info (по умолчанию), warn, error.

**Ротация:** Максимальный размер 100MB, до 5 файлов (`agent.log`, `agent.log.1`, etc.).

---

## 🔨 Сборка Агента

### Статическая Сборка для Linux

Агент собирается как **один статический бинарник** без зависимостей, который работает на любом Linux дистрибутиве (Ubuntu, Debian, CentOS, RHEL, etc.).

**Сборка:** `cargo build --release --target x86_64-unknown-linux-musl` после установки `musl-tools` и добавления target. Результат: один статический бинарник ~5-10MB (сжатый), ~15-20MB (несжатый).

**Оптимизация:** `opt-level = "z"`, `lto = true`, `codegen-units = 1`, `strip = true` в `[profile.release]`.

**Проверка:** `ldd` должен вывести "not a dynamic executable", `file` — "statically linked".

**CI/CD:** Автоматическая сборка через GitHub Actions при создании тега `agent-v*`, включает установку musl, сборку, сжатие и вычисление SHA256 checksum.

---

## Установка и развертывание

### 1. Автоматическая Установка при Добавлении Сервера

**Процесс установки через BullMQ:**

Когда пользователь добавляет сервер в Control Plane, система автоматически:

1. Создает задачу установки агента в BullMQ очередь `agent-installation`
2. Worker подключается через SSH, проверяет Docker, определяет архитектуру
3. Генерирует токен агента и загружает статический бинарник
4. Устанавливает systemd service и запускает агента
5. Регистрирует агента в Control Plane

**Мониторинг:** Real-time обновления через SSE endpoint `/servers/:serverId/install-status`

### 2. Альтернативные Способы Установки

**Docker Compose:** Агент может быть запущен в контейнере с монтированием Docker socket и необходимыми volumes.

**Systemd Service:** Для production рекомендуется установка как systemd service с настройками безопасности (NoNewPrivileges, ограничения ресурсов, read-only paths).

**Ручная установка:** Для тестирования можно установить вручную — загрузить бинарник, создать пользователя, настроить токен и запустить через systemd.

**Регистрация:** После установки агент автоматически регистрируется в Control Plane через gRPC при первом подключении.

---

## 🧪 Тестирование и Валидация

Агент покрыт тестами на всех уровнях:

- **Unit Tests** — тестирование отдельных компонентов (Docker Swarm Manager, Update Manager)
- **Integration Tests** — тестирование полного workflow (регистрация, команды, статусы)
- **E2E Tests** — полный цикл деплоя от Control Plane до Docker Swarm

---

## Телеметрия и мониторинг

### Метрики Агента

- **Connection Status:** Подключен/отключен (gRPC)
- **Last Heartbeat:** Время последнего heartbeat
- **Commands Executed:** Количество выполненных команд
- **Errors:** Количество ошибок
- **Uptime:** Время работы агента
- **Current Version:** Текущая версия агента
- **Update Status:** Статус последнего обновления (success/failed/rolled_back)
- **Update Duration:** Время последнего обновления

### Метрики Сервера

- **CPU Usage:** Использование CPU
- **Memory Usage:** Использование памяти
- **Disk Usage:** Использование диска
- **Network I/O:** Сетевой трафик

### Метрики Проектов (Docker Swarm)

- **Service Status:** Статус сервисов в Swarm
- **Replicas:** Количество реплик
- **Health Checks:** Результаты health checks
- **Resource Usage:** Использование ресурсов контейнерами

---

## Процесс деплоя с агентом

### Старый Процесс (без агента):

```
Control Plane → BullMQ → Worker → SSH → Server → docker compose up
```

**Проблемы:**

- Одноразовые SSH подключения (задержки)
- Нет постоянного мониторинга
- Нет локального сбора телеметрии
- Нет автономной работы
- Docker Compose не поддерживает продвинутую оркестрацию

### Новый Процесс (с Runner Agent):

```
Control Plane → BullMQ → Worker → gRPC + Kafka → Runner Agent → Docker Swarm
                                                                    ↓
                                                          Телеметрия → Control Plane (gRPC)
```

**Преимущества:**

- ✅ **gRPC + Protobuf** — типобезопасное общение (уже используется в системе)
- ✅ **Kafka** — надежная доставка команд и событий, гарантия доставки
- ✅ **Docker Swarm** — продвинутая оркестрация (rolling updates, health checks, scaling)
- ✅ **Rust** — максимальная производительность, низкое потребление ресурсов
- ✅ **Локальный сбор телеметрии** — метрики собираются на сервере
- ✅ **Автономная работа** — Agent может работать даже при проблемах с Control Plane
- ✅ **Real-time мониторинг** — метрики и логи в реальном времени

**Детальный процесс:**

1. **Control Plane** создает задачу деплоя в BullMQ
2. **Deployment Worker** обрабатывает задачу
3. **Worker** отправляет команду `deploy` через **gRPC** к Runner Agent
4. **Worker** публикует событие деплоя в **Kafka** (для надежности)
5. **Runner Agent** получает команду через **gRPC** или **Kafka**
6. **Runner Agent** управляет **Docker Swarm** локально:
   - Создает/обновляет Swarm stack
   - Выполняет rolling updates
   - Проверяет health checks
   - Масштабирует сервисы при необходимости
7. **Runner Agent** отправляет статус обратно в Control Plane через **Kafka**
8. **Runner Agent** начинает сбор телеметрии и отправку через **gRPC**

Подробнее: [`ARCHITECTURE`](./ARCHITECTURE#phase-5-deployment-с-axion-runner-agent) и [`TECHNICAL_DETAILS`](./TECHNICAL_DETAILS#-деплой-на-серверы-клиентов-через-axion-runner-agent)

---

## Локальная интеграция с Tauri Client

### Концепция

Runner Agent может работать **локально** на компьютере разработчика и общаться напрямую с **Axion Tauri Desktop Client** через нативные интерфейсы операционной системы. Оба компонента написаны на **Rust**, что позволяет им общаться напрямую без HTTP/gRPC overhead.

**Преимущества локальной интеграции:**

- ⚡ **Мгновенная реакция** — нет сетевых задержек, прямая передача данных
- 🔒 **Безопасность** — доступ только с локальной машины, нет сетевых уязвимостей
- 📦 **Минимальный overhead** — нет HTTP/gRPC заголовков, бинарный протокол
- 🚀 **Высокая производительность** — прямая передача данных через IPC
- 🛠️ **Простота отладки** — локальная работа, легче отлаживать
- 💾 **Эффективность памяти** — shared memory для больших данных (опционально)

### Механизмы Коммуникации

#### 1. Unix Domain Sockets (Linux/macOS)

**Самый быстрый способ для Linux/macOS:**

```rust
// Runner Agent (Rust) - IPC Server
use tokio::net::UnixListener;

async fn start_local_ipc_server() -> Result<()> {
    let socket_path = "/tmp/axion-runner-agent.sock";

    // Удалить старый socket если существует
    let _ = std::fs::remove_file(socket_path);

    let listener = UnixListener::bind(socket_path)?;

    // Установить права доступа (только владелец)
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt;
        std::fs::set_permissions(socket_path, PermissionsExt::from_mode(0o600))?;
    }

    loop {
        let (stream, _) = listener.accept().await?;
        // Обработка команд от Tauri Client
        tokio::spawn(handle_client(stream));
    }
}

async fn handle_client(mut stream: UnixStream) -> Result<()> {
    // Чтение команды от Tauri Client
    let command: RunnerCommand = bincode::deserialize_from(&mut stream)?;

    // Обработка команды
    let response = match command {
        RunnerCommand::Deploy { project_id, docker_stack_yml, env_vars } => {
            // Деплой через Docker Swarm
            deploy_project(project_id, docker_stack_yml, env_vars).await?
        }
        RunnerCommand::GetStatus { project_id } => {
            // Получить статус проекта
            get_project_status(project_id).await?
        }
        // ... другие команды
    };

    // Отправить ответ обратно
    bincode::serialize_into(&mut stream, &response)?;

    Ok(())
}
```

#### 2. Named Pipes (Windows)

**Аналог Unix sockets для Windows:**

```rust
// Runner Agent (Rust) - IPC Server для Windows
use tokio::net::windows::named_pipe::{ServerOptions, NamedPipeServer};

async fn start_local_ipc_server() -> Result<()> {
    let pipe_name = r"\\.\pipe\axion-runner-agent";

    loop {
        let server = ServerOptions::new()
            .first_pipe_instance(true)
            .create(pipe_name)?;

        // Ожидание подключения Tauri Client
        server.connect().await?;

        // Обработка команд
        tokio::spawn(handle_client(server));
    }
}
```

#### 3. gRPC Локально (Опционально)

**Для полной совместимости с Control Plane протоколом:**

```rust
// Runner Agent (Rust) - gRPC Server для локального использования
use tonic::transport::Server;

async fn start_local_grpc_server() -> Result<()> {
    let addr = "127.0.0.1:50051".parse()?;
    let runner_service = RunnerAgentService::default();

    Server::builder()
        .add_service(RunnerAgentServer::new(runner_service))
        .serve(addr)
        .await?;

    Ok(())
}
```

### Протокол Коммуникации

**Бинарный протокол через IPC:**

```rust
// Команда от Tauri Client к Runner Agent
#[derive(Serialize, Deserialize)]
pub enum RunnerCommand {
    Deploy {
        project_id: String,
        docker_stack_yml: String,
        env_vars: HashMap<String, String>,
    },
    GetStatus {
        project_id: String,
    },
    GetMetrics {
        project_id: String,
    },
    ScaleService {
        project_id: String,
        service_name: String,
        replicas: u32,
    },
    StopProject {
        project_id: String,
    },
    StreamMetrics {
        project_id: String,
    }, // Stream метрик в реальном времени
}

// Ответ от Runner Agent к Tauri Client
#[derive(Serialize, Deserialize)]
pub enum RunnerResponse {
    DeployStatus {
        status: DeployStatus,
        message: String,
    },
    ProjectStatus {
        services: Vec<ServiceStatus>,
    },
    Metrics {
        metrics: ProjectMetrics,
    },
    MetricsStream {
        metrics: ProjectMetrics,
    }, // Поток метрик
    Error {
        error: String,
    },
}
```

**Сериализация:**

- **bincode** — быстрая бинарная сериализация для IPC (по умолчанию)
- **Protobuf** — для gRPC (опционально, для совместимости)
- **JSON** — для отладки и логирования

### Режимы Работы Агента

Runner Agent может работать в двух режимах:

1. **Облачный режим** — подключение к Control Plane через gRPC + Kafka
2. **Локальный режим** — подключение к Tauri Client через IPC/Unix Sockets

Агент может работать в обоих режимах одновременно, обрабатывая команды от обоих источников.

### Конфигурация Локального Режима

```toml
# /etc/axion/agent.toml или ~/.axion/agent.toml
[local]
enabled = true
ipc_socket_path = "/tmp/axion-runner-agent.sock"  # Linux/macOS
ipc_pipe_name = r"\\.\pipe\axion-runner-agent"    # Windows
grpc_local_port = 50051                           # Опционально

[local.security]
socket_permissions = 0o600  # Только владелец может подключаться
allowed_users = ["user1", "user2"]  # Список разрешенных пользователей (опционально)
```

### Использование в Tauri Client

**Подключение к локальному Runner Agent:**

```rust
// Tauri Client (Rust)
use tokio::net::UnixStream;

async fn connect_to_local_runner_agent() -> Result<RunnerAgentClient> {
    let socket_path = "/tmp/axion-runner-agent.sock";
    let stream = UnixStream::connect(socket_path).await?;

    RunnerAgentClient::new(stream)
}

// Отправка команды деплоя
async fn deploy_locally(project_id: String) -> Result<()> {
    let client = connect_to_local_runner_agent().await?;

    let command = RunnerCommand::Deploy {
        project_id: project_id.clone(),
        docker_stack_yml: generate_docker_stack(&project)?,
        env_vars: project.env_vars,
    };

    let response = client.send_command(command).await?;

    match response {
        RunnerResponse::DeployStatus { status, message } => {
            println!("Deploy status: {:?}, message: {}", status, message);
        }
        RunnerResponse::Error { error } => {
            eprintln!("Error: {}", error);
        }
        _ => {}
    }

    Ok(())
}
```

### Преимущества для Разработки

**Локальная интеграция идеальна для:**

- 🛠️ **Локальной разработки** — разработка проектов без интернета
- ⚡ **Быстрого прототипирования** — мгновенная генерация и тестирование
- 🧪 **Экспериментов** — безопасное тестирование без влияния на production
- 🔐 **Приватных проектов** — проекты остаются на вашем компьютере
- 🐛 **Отладки** — легче отлаживать локально, чем на удаленном сервере

Подробнее: **[`TAURI_CLIENT.md`](./TAURI_CLIENT)** ⭐ — Детальная архитектура Tauri клиента с локальным режимом и нативной интеграцией с Runner Agent.

---

## Система автообновления

### Концепция

Runner Agent поддерживает автоматическое обновление с Control Plane серверов. Система обеспечивает безопасное обновление с проверкой целостности, graceful shutdown и rollback механизмом.

### Компоненты

1. **Update Manager** — управление обновлениями
2. **Version Checker** — проверка доступных версий
3. **Download Manager** — безопасная загрузка обновлений
4. **Integrity Verifier** — проверка целостности (SHA256)
5. **Update Installer** — установка обновлений
6. **Rollback Manager** — откат при проблемах

### Каналы Обновлений

Поддерживаются каналы: **Stable** (по умолчанию), **Beta**, **Alpha**.

### Процесс Обновления

1. **Проверка доступных обновлений** — через gRPC запрос к Control Plane
2. **Загрузка обновления** — через HTTPS с проверкой TLS и SHA256 checksum
3. **Graceful shutdown** — остановка телеметрии, завершение команд, закрытие соединений
4. **Установка** — сохранение backup, атомарная замена бинарника, перезапуск
5. **Rollback** — автоматический откат при проблемах из сохраненных backup'ов
6. **Автоматическая проверка** — каждые 3600 секунд, поддержка обязательных обновлений

### Безопасность Обновлений

1. **TLS Verification** — строгая проверка TLS сертификатов при загрузке
2. **Checksum Verification** — проверка SHA256 для целостности
3. **Signed Updates** — цифровая подпись обновлений (опционально)
4. **Rollback Protection** — сохранение предыдущих версий для отката
5. **Update Logging** — логирование всех операций обновления

### Метрики Обновлений

- **Update Success Rate** — процент успешных обновлений
- **Update Duration** — время обновления
- **Rollback Rate** — частота откатов
- **Version Distribution** — распределение версий агентов

---

## Улучшения надежности и безопасности

Агент реализует следующие паттерны надежности:

1. **Circuit Breaker** — защита от каскадных сбоев при проблемах с gRPC
2. **Retry с Exponential Backoff** — повторные попытки с увеличивающейся задержкой
3. **Health Checks** — проверка здоровья Docker, gRPC, Kafka компонентов
4. **Structured Logging** — JSON логи для парсинга и анализа
5. **Rate Limiting** — ограничение частоты запросов

Все паттерны реализованы на Rust с использованием стандартных библиотек.

---

## Управление версиями в Control Plane

**Database Schema:** Таблицы `agent_versions` (версии с каналами, download_url, checksum) и `agent_update_history` (история обновлений агентов).

**API Endpoints:** `POST /api/agents/:agentId/check-update`, `POST /api/agents/:agentId/update`, `GET /api/agents/:agentId/update-history`.

**Процесс публикации:** Сборка бинарника → вычисление SHA256 → загрузка на CDN → регистрация в БД → уведомление агентов через heartbeat/Kafka.

---

## 🚀 Roadmap

Детальный план реализации: [`TODO.md`](./TODO). Основные фазы: базовая функциональность (gRPC, Kafka, Docker Swarm), расширенная функциональность (telemetry, health checks, автообновление), оптимизация и надежность (circuit breaker, retry, structured logging), безопасность (TLS, изоляция, минимальные привилегии).

---

## Troubleshooting

**Частые проблемы:**

1. **Агент не подключается** — проверка gRPC соединения, токена, DNS, логов
2. **Kafka Consumer Lag** — проверка через Kafka CLI, перезапуск consumer
3. **Docker Swarm проблемы** — проверка статуса, инициализация Swarm
4. **Высокое потребление ресурсов** — проверка метрик, ограничение в systemd
5. **Проблемы с обновлением** — проверка backup'ов, ручной rollback, проверка checksum

**Диагностические команды:** `systemctl status`, `journalctl -u axion-agent -f`, `curl http://localhost:8080/metrics`, `curl http://localhost:8080/health`, `/usr/local/bin/axion-agent --version`

---

## ⚡ Оптимизация Производительности

Агент использует следующие оптимизации:

1. **Batch отправка метрик** — группировка метрик для уменьшения количества запросов
2. **Connection Pooling** — переиспользование gRPC соединений
3. **Кэширование** — кэширование статуса Docker Swarm сервисов с TTL
4. **Асинхронная обработка** — параллельная обработка команд через tokio

---

## Best Practices

### 1. Развертывание

- ✅ Использовать systemd для управления жизненным циклом
- ✅ Настроить автоматический restart при сбоях
- ✅ Ограничить ресурсы (CPU, Memory)
- ✅ Использовать read-only файловую систему где возможно
- ✅ Минимальные Linux capabilities

### 2. Безопасность

- ✅ Хранить токены в зашифрованном виде
- ✅ Использовать TLS для всех соединений
- ✅ Регулярно обновлять агент
- ✅ Мониторить подозрительную активность
- ✅ Использовать firewall для ограничения доступа

### 3. Мониторинг

- ✅ Настроить Prometheus scraping
- ✅ Настроить alerting rules
- ✅ Мониторить метрики агента
- ✅ Отслеживать ошибки и retry
- ✅ Логировать все критические операции

### 4. Резервное Копирование

- ✅ Регулярно делать backup конфигураций проектов
- ✅ Хранить backup'ы в отдельном месте
- ✅ Тестировать восстановление из backup
- ✅ Ротация старых backup'ов

### 5. Обновления

- ✅ Использовать stable канал для production
- ✅ Тестировать обновления на staging
- ✅ Мониторить процесс обновления
- ✅ Иметь план rollback
- ✅ Документировать изменения в версиях

---

## 📚 Связанные Документы

- **[`ARCHITECTURE.md`](./ARCHITECTURE)** ⭐ — Полная архитектура Axion
  - Phase 5: Deployment описывает интеграцию Runner Agent в процесс деплоя
  - Раздел "Клиенты" описывает веб и Tauri клиенты
- **[`TECHNICAL_DETAILS.md`](./TECHNICAL_DETAILS)** — Технические детали
  - Раздел "Деплой на Серверы Клиентов" описывает процесс деплоя через Runner Agent
  - Раздел "Система Метрик" описывает сбор метрик через Runner Agent
- **[`TAURI_CLIENT.md`](./TAURI_CLIENT)** ⭐ — Архитектура Axion Tauri Desktop Client
  - Раздел "Нативная Интеграция с Runner Agent" описывает IPC коммуникацию
  - Раздел "Локальный Деплой через Runner Agent" описывает локальный деплой
- **[`COMPARISON.md`](./COMPARISON)** — Сравнение с CapRover и Convex
  - Runner Agent отмечен как реализованный компонент

---

## Ключевые выводы

**Runner Agent — это критически важный компонент для:**

1. **Эффективного деплоя** — gRPC + Kafka вместо одноразовых SSH
2. **Продвинутой оркестрации** — Docker Swarm вместо Docker Compose
3. **Real-time мониторинга** — метрики и логи в реальном времени
4. **Автономной работы** — Agent может работать даже при проблемах с Control Plane
5. **Производительности** — Rust для максимальной производительности и надежности
6. **Универсальности** — один статический бинарник для всех Linux дистрибутивов
7. **Простой установки** — автоматическая установка при добавлении сервера
8. **Безопасности** — проверка целостности, TLS, изоляция, минимальные привилегии
9. **Надежности** — circuit breaker, retry, graceful shutdown, rollback
10. **Observability** — structured logging, Prometheus метрики, health checks
11. **Локальной интеграции** ⭐ — нативная интеграция с Tauri Client через IPC/Unix Sockets для локальной разработки

**Технологический стек:**

- **Rust + musl** — статическая сборка для работы на любом Linux
- **gRPC + Protobuf** — типобезопасное общение с Control Plane (уже используется в системе)
- **Kafka** — надежная доставка команд и событий, гарантия доставки
- **Docker Swarm** — продвинутая оркестрация контейнеров
- **BullMQ** — автоматическая установка при добавлении сервера
- **IPC/Unix Sockets** ⭐ — нативная коммуникация с Tauri Client для локальной работы
- **Best practices** — health checks, graceful shutdown, retry, circuit breaker, observability
- **Автообновление** — безопасное обновление с проверкой целостности и rollback
- **Prometheus** — экспорт метрик для мониторинга
- **Structured Logging** — JSON логи для парсинга и анализа

**Архитектурные преимущества:**

- ✅ **Модульность** — каждый компонент изолирован и тестируем
- ✅ **Масштабируемость** — легко добавлять новые функции
- ✅ **Надежность** — обработка ошибок на всех уровнях
- ✅ **Безопасность** — многоуровневая защита
- ✅ **Observability** — полная видимость работы агента
- ✅ **Автоматизация** — автообновление, backup, восстановление
- ✅ **Универсальность** — один бинарник для всех Linux дистрибутивов
- ✅ **Простота установки** — автоматическая установка при добавлении сервера
- ✅ **Локальная интеграция** ⭐ — нативная интеграция с Tauri Client для локальной разработки

---

_Этот документ описывает архитектуру Axion Runner Agent для эффективного управления Docker Swarm на серверах клиентов. Runner Agent написан на Rust, использует gRPC + Kafka для общения с Control Plane и интегрирован в процесс деплоя._
